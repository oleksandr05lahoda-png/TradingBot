# Хэндоф — состояние на 14.08.2026 (ревизия 5)

Вставь это в новый чат целиком.

## 🟢 РЕВИЗИЯ 5 — ЧИТАЙ ЭТОТ БЛОК ПЕРВЫМ, ниже более старое

**Направленный поиск закрыт целиком.** 14.08 измерены последние две неизмеренные направленные
идеи. Обе провалили предрегистрированный порог (holdout, хедж по бете BTC, t ≥ 2.0):

| идея | holdout хедж. альфа | t | вердикт |
|---|---|---|---|
| медленный TSMOM (L=90, недельный ребаланс) | +27.2%/год | **0.94** | FAIL → ярус C |
| шорт под token-анлоки (H=14) | +46.0%/год | **1.13** | FAIL → ярус C |

Расчёты: `analysis/tsmom/` и `analysis/unlocks/`, правила зафиксированы в `PREREG.md` каждой папки
ДО прогона, итоги — в `SUMMARY.md`. **Неизмеренных направленных идей в каталоге не осталось.**

**Три вещи из этого замера, которые меняют работу дальше:**

1. **Плацебо-контроль обязателен на любой событийной гипотезе.** 200 случайных книг (те же дни
   входа, те же горизонты, случайные монеты вместо «событийных») дали в holdout медиану
   **+19.8%/год** хеджированной альфы. Настоящая книга анлоков (+46.0%) — 90-й процентиль этого
   фона. То есть в 2025–2026 хеджированный шорт ЛЮБЫХ альтов «работал», и без контроля это читается
   как эдж события. Прошлые событийные замеры лаборатории такого контроля не имели — сравнивать
   надо не с нулём, а с фоном.
2. **Односторонняя форма ≠ хеджированное число.** Все положительные цифры выше — при бете −0.27
   (TSMOM) и −1.33 (анлоки). Без второй ноги анлок-шорт дал −43.6%/год на дизайне и +65.6%/год на
   holdout: одна и та же машина, знак задаёт рынок. Одним ордером «альфу» из таблицы не забрать.
3. **Survivorship-дыра прошлых замеров невелика.** Публичный архив `data.binance.vision` хранит
   делистнутые контракты; из 187 мёртвых USDT-перпов крипты оказалось всего **32** (остальные —
   токенизированные акции 2026 и сырьё). Дыра закрыта в обоих новых замерах, но прошлые выводы она
   бы не перевернула. Полный кэш 2019-09 → 2026-08 по 672 символам лежит в `analysis/tsmom/cache/`
   (132 МБ) — цены и фандинг, переиспользуй, а не качай заново.

Остальное из ревизии 4 в силе: кондиционный funding-carry — единственный выживший механизм,
`funding-radar` / `carry-paper` / `lab-watchdog` крутятся сами, ключ нового Supabase ждёт владельца.

### Дополнение 14.08 вечер — живой demo-форвард по решению владельца

**Владелец, увидев все замеры, решил торговать направленно на demo: ТОЛЬКО ЛОНГИ.** Числа ему
предъявлены (long-only holdout: альфа отрицательна во всех четырёх окнах), решение его,
зафиксировано в шапке каждой генерируемой книги. Не переубеждать повторно — предъявлено трижды.

- **Живой процесс на ноуте владельца**: detached `gradlew run`, книга
  `analysis/forward/book_long_2026-08-14.txt` (38 лонг-сигналов → 18 открыто гейтами),
  `MAX_POSITIONS=20` (env-override, коммит 727741c), `DEFAULT_LEVERAGE=2`,
  лог `analysis/forward/bot_long.err.log`. Ноут не должен спать.
  ⚠️ Рестарт с открытыми позициями → reconciliation halt открытий (закрытия работают);
  пересбор книги = CLOSE-скрипт → проверка flat → новый скан → чистый старт.
- **Сканер** `tools/scanner/scan.py`: `--side long|short|both`, `--universe turnover|cap`,
  `--top`, `--max-signals`. Правило: знак 90д доходности, стоп из ATR(14).
- **Решение по вселенной**: с понедельника 18.08 пересбор на `--universe cap --top 100 --side long`
  (cap top-50 сегодня дал бы всего 9 кандидатов). Пересбор ручной.
- **Условия остановки форварда, зафиксированы заранее**: просадка счёта −8%; дневной кил-свитч
  3 раза за 10 дней; реальные косты вдвое выше модели 10 б.п.
- **n8n**: `crypto-digest` и `execution-cost-probe` ДЕАКТИВИРОВАНЫ 14.08 (оба нерабочие без ключа
  базы, probe падал каждые 15 мин). Вернуть после ключа + переписывания RPC `probe_tick`/
  `crypto_digest` под новую базу. Активны: `funding-radar`, `carry-paper`, `lab-watchdog`.
- **Замеры 14.08 сверх ревизии 5**: режим-фильтр по BTC убивает альфу (+27.2→−2.3); адаптивный
  выбор окна = 42-й процентиль случайного; long-only альфа < 0 во всех окнах; short-only
  t=2.13 — единственная клетка >2 за весь проект, но post-hoc при ~30 проверках за день, торговать
  без свежей предрег-проверки нельзя; «в плюсе за 14 дней» отличает стратегию от монетки с
  вероятностью 53.6%; до t=2 нужно 10.9–21.9 года. Скрипты: `analysis/tsmom/{speed,sides,regime}.py`.

---

## 🟢 РЕВИЗИЯ 4 — предыдущее состояние

**Стратегии искали и измерили. Итог — в [STRATEGY_CATALOG.md](STRATEGY_CATALOG.md), расчёты в
`analysis/` (в gitignore).** Закрыто по числам, НЕ переоткрывать: свечи/уровни/просадки, шорт
листингов (t=−0.07; со стопами на holdout t=−0.89), интрадей-окна, гриды/DCA/копитрейдинг,
и рецепт «самообучающегося ИИ-бота» из популярных роликов — его «самообучение» проигрывает
подбрасыванию монетки (35-й процентиль из 300 случайных сидов, `analysis/selflearn`).

**Выжил один механизм — кондиционный funding-carry, и он измерен честно** (1.23 млн фактических
событий фандинга, `analysis/carry`): медиана **+0.085%/мес** при честной книге капитала, по годам
**+11.1% → +2.1% → +0.4%**, а за последние 12 месяцев **≈ 0.00%** при 6 убыточных месяцах из 12.
Тейкерскими костами убыточны все правила. Не обещать владельцу процентов — числа выше.

**Живёт в n8n (крутится само, ключей не требует):**
- `funding-radar` (vlSeX1MjhdTYf75S) — раз в час, алерт при ставке ≥0.04%/интервал;
- `carry-paper` (bFFDZ4K5FZFkn2nI, версия 994570e3) — бумажные эпизоды: спот-гейт fail-closed,
  фандинг только по фактическим сеттлментам, выход только при отрицательной ставке (правило R2);
- `lab-watchdog` (uoNQxhXYHcv4GJGt) — сторож; окна повтора временно 24ч, вернуть на 4ч.

**Ветки записи в Supabase спят**, пока credential «Supabase crypto» смотрит на удалённый проект.

**Правило работ владельца:** n8n → Supabase → код → Railway 24/7 ТОЛЬКО после доказанной прибыльной
стратегии. Её нет. Не подталкивать к выкатке и живым деньгам.

**Не проверено, единственное оставшееся:** шорт под token-анлоки (ярус B каталога).
→ ⚠️ УСТАРЕЛО: измерен 14.08, FAIL, см. ревизию 5 вверху. Ровно как и медленный TSMOM, которого
в этом списке не было, хотя он тоже стоял неизмеренным.

---

Что изменилось в ревизии 3: проект `ptlxojilgqylzxshsrlm` **удалён владельцем** (после аварии 10.08
он так и не вышел из петли восстановления; пауза откатилась, бесплатный план диск расширить не даёт).
Новый проект — `crypto-listings`, ref **`nlvxkeckbpxsblnufwwa`**, тот же регион, $0/мес.

---

Продолжаем работу над `C:\Users\Asus_F15\IdeaProjects\TradingTelegramBot`, ветка
`feature/testnet-risk-core` (main НЕ трогать до ревью).

## 🟡 ПЕРВЫМ ДЕЛОМ: новая база поднята, ждёт переключения ключей

На `nlvxkeckbpxsblnufwwa` прогнаны две миграции (`bootstrap_core_stack`,
`paper_signals_prereg_journal`) и проверено запросами 12.08: 14 таблиц, ни одной пропущенной;
17 строк-заготовок в `pipeline_heartbeat`; 4 индекса на `bot_orders`; RLS на всех таблицах
(политик нет — доступ только сервисным ключом, это задумано); CHECK на `status` принимает все
девять значений; минимальная вставка в очередь проходит с дефолтами `pending`/`testnet=true`;
триггер предрегистрации отвергает вставку сигнала вместе с исходом; советники безопасности — без
WARN. Боевой контур (`sleeve_gates`, `enqueue_live_order`) **сознательно не создан** — см.
sql/bootstrap_fresh_project.sql, шапка.

**ДАННЫХ НЕТ.** Схема есть, содержимого нет: свечи и фандинг качаются заново, реестр лабы и
форвард dip_bounce обнулены, история зонда потеряна. pg_cron-джобов нет — их определения не
сохранились нигде, заводить заново по мере надобности.

**Осталось руками владельца** (ключи в переписку не принимаются) — новый URL
`https://nlvxkeckbpxsblnufwwa.supabase.co`, ключи в Dashboard → Settings → API:
1. `local.env`: `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`;
2. Railway → TradingBot: `SUPABASE_URL`, `BRIDGE_SUPABASE_KEY`, `SUPABASE_KEY`;
3. n8n → credential «Supabase crypto»: URL + сервисный ключ.
Ключ нового формата `sb_secret_…` бот теперь переваривает (fix cf64061); публикуемый ключ НЕ
годится — RLS вернёт пустоту без ошибки.

**Урок аварии остаётся в силе:** субагентам запрещать `CREATE TABLE` в проде — только SELECT и CTE.

### ⏳ ВРЕМЕННО — вернуть после переключения ключей и первых кронов

Окна повтора тревог расширены **с 4ч до 24ч**, чтобы известная авария не давала 14 сообщений в
сутки и канал не замьютили. Ничего не выключено: любая НОВАЯ проблема, смена состава проблем и
возврат в норму по-прежнему уходят на ближайшем прогоне — расширен только повтор неизменного
состояния. Тронуто шесть мест в трёх воркфлоу (12.08, версии `91113c0c` / `b8035b93` / `ef79e854`):

| воркфлоу | ноды | что вернуть |
|---|---|---|
| `lab-watchdog` | `Evaluate Freshness` (2 места), `Dedup Unreachable` | `24 * 3600 * 1000` → `4 * 3600 * 1000` |
| `execution-cost-probe` | `Dedup failure alert`, `Probe health check` | `REPEAT_WINDOW` → `FOUR_H`, и текст «раза в 24 часа» → «раза в 4 часа» |
| `crypto-digest` | `Digest DOWN` | только число в `REPEAT_WINDOW` |

⚠️ Для `crypto-digest` откат версией **НЕЛЬЗЯ**: в той же правке ему добавлен дедуп, которого у него
не было никогда, и нода `Commit Digest State`. Возврат версии снесёт их вместе с окном — там правится
только число. У двух других откат версией допустим.

⚠️ Побочное: описание воркфлоу `execution-cost-probe` всё ещё говорит «раз в 4ч» — поправить вместе с
окном.

## 🔴 Второе: боевой контур — ОТКРЫТИЯ заперты кодом, ЗАКРЫТИЯ не заперты никогда

На Railway крутится коммит `9739f5c` (деплой от 28.07.2026, процесс с тех пор не перезапускался).
**Открыть боевую позицию он не может.** Замок в коде и двойной:

1. `executionAllowed()` возвращает false, потому что `BINANCE_USE_TESTNET` не задана — отсутствие
   переменной трактуется как отказ, а не как дефолт;
2. второе условие требует, чтобы хост исполнителя входил в белый список демо-хостов
   (`demo-fapi.binance.com`, `testnet.binancefuture.com`, `demo.binance.com`). `fapi.binance.com`
   в него не входит.

`BRIDGE_ALLOW_REAL` в Railway задан и включён, но по коду **не разрешает ничего**: он читается
только затем, чтобы залогировать, что проигнорирован.

⚠️ Стартовый баннер
`[Bridge] polling host=fapi.binance.com *** NOT A DEMO HOST *** opens=LOCKED closes=ALWAYS allowReal=true`
печатается **один раз за жизнь процесса** (флаг `bannerLogged`) — в хвосте лога его нет, не ищи там и
не делай из его отсутствия вывод, что мост мёртв. Каждые 30 секунд повторяется другая пара строк, и
она же и есть живое доказательство, что замок держит:
`BRIDGE_ALLOW_REAL=1 IGNORED: real endpoint is locked in code` и
`BLOCKED: BINANCE_USE_TESTNET is not set — refusing to trade rather than silently choosing an endpoint`.

Сделка **−15.40%** (`bot_orders` id 1, реальный XRPUSDT $5.17) была **08.07 — ДО появления замка**.
Она не доказательство того, что контур выстрелит снова.

**Но пути ЗАКРЫТИЯ не заперты — `closes=ALWAYS`, и это сделано намеренно:** замок, однажды
накрывший весь цикл, сломал тайм-стопы (боевая позиция переставала закрываться вообще).
Практический вывод: **когда база поднимется, бот в течение 30 секунд исполнит боевыми ключами
reduce-only ордера из `bot_orders` со `status=close_requested` и `testnet=false` — без
подтверждения.** Возрастного гейта у закрытий нет (у открытий есть, 15 минут).

Оговорка, которая на деле подтверждается: весь мост сидит за `SUPABASE_BRIDGE_ENABLED` (дефолт `0`,
при выключенном мосте `poll()` вообще не планируется). В Railway переменная задана, и её значение
видно косвенно, но надёжно — логи опроса идут каждые 30 секунд, значит мост включён. А открытия так и
останутся лежать: проверка «pending старше 15 минут» сама сидит внутри запертого пути, поэтому
строки просто копятся нетронутыми.

Enqueue-контур снаружи проекта никто не выключал: `sleeve_gates.long_v2.live = true` → крон
`long-v2-scan` (:41) → `enqueue_live_order` (цена с боевого `fapi.binance.com`) → строка
`testnet=false`. Снять (решение владельца, само не делать, и только после подъёма базы):
`update sleeve_gates set criteria = jsonb_set(criteria,'{live}','false') where sleeve='long_v2';`

## 🔴 Третье: `BINANCE_USE_TESTNET` в Railway — не задавать и не удалять

У переменной ДВА эффекта, и второй не очевиден: (а) она участвует в гейте открытий; (б) она
подставляется во **все** фильтры PostgREST моста (`testnet=eq.<флаг>`).

- **Выставить в `1`** → мост переключится на `testnet=true`, боевые `close_requested` перестанут
  читаться, и тайм-стопы боевых позиций молча перестанут работать. Вдобавок demo-ключей
  (`BINANCE_TESTNET_API_KEY` / `BINANCE_TESTNET_API_SECRET`) в Railway нет — executor вообще не
  инициализируется, `closePosition` выйдет на первой строке.
- **Выставить в `0`** → открытия это НЕ разрешает: второе условие гейта всё равно упрётся в хост.

⚠️ В истории лежит коммит `6c40c02` (16.06.2026), прямо предписывающий эту переменную из Railway
удалить. **Следовать этой инструкции нельзя**: она опиралась на гейт `LIVE_TRADING_ARMED`.

`LIVE_TRADING_ARMED` **ничего не гейтит на пути ордеров** — хотя в коде он есть, и это важно не
перепутать. На `main` он читается из окружения (`BotMain.java:122-123`, применяется на `:478`; ещё
`BinanceTradeExecutor.java:55` и `:99`) — это гейт свечного стека, вырезанного из текущей ветки.
В переменных Railway его нет вообще, то есть читается как `0`. Живой путь ордеров
(`SupabaseSignalBridge`) его не спрашивает ни разу. Если он всплывёт в чьей-то памяти или в старой
доке как «предохранитель боевой торговли» — это не действующий механизм, он ничего не защищает.

## Что построено

Риск-движок + слой исполнения для Binance USDⓈ-M **testnet**. Стратегии внутри нет и быть не
должно. 44 файла в `src/main`, **199 тестов** в 28 файлах (пересчитано 11.08: было 193 в 27, плюс
6 на молчаливое отключение алертов), точка входа `com.bot.app.TestnetBot`.

Рамки зашиты структурно: боевого URL нет и тест роняет сборку при его появлении; `MAX_LEVERAGE = 5`
и потолок риска 1% — константы; позиция без стопа не конструируется; `SignalSource` имеет ровно две
реализации и тест это считает.

**Проверено живой биржей:** цепочка `n8n → Supabase → бот → demo-fapi → стоп + 2 тейка → закрытие`.
Проверка стопа поимённым запросом закрыта и подтверждена живой сделкой (`algoStatus:"NEW"` читается,
снятие стопа даёт `POSITION_WITHOUT_STOP` + halt).

**Стоимость, два замера:** ~10 б.п. за круг (8 б.п. комиссии + 2.4 б.п. проскальзывание).

## ⭐ ГЛАВНОЕ: price action закрыт окончательно

**~50 конструкций, 700 000+ симулированных сделок, НОЛЬ с положительной хеджированной альфой.**

Закрыты: все свечные паттерны (молот t=−3.64, поглощения t=−4.96, доджи t=−3.76, пронзающая
t=−4.12, звёзды, пинцеты, трёхбарные, сжимающиеся), уровни, пробои, ложные пробои (t=−12.5),
диапазоны (t=−12.7), VWAP-полосы, MACD, структура, отбор лучших, обе стороны, три таймфрейма.

**Вскрыт механизм иллюзии — три независимых способа выглядеть прибыльным при нулевом эдже:**

1. **Бета BTC 1.1–1.9 на каждой строке.** Любое «покупай слабость на альтах» = плечевой лонг BTC.
   Убило vwap_dip (сырое t=4.81 → hedged t=1.14) и на 2/3 dip_bounce.
   **Всегда считать hedged = day_ret − beta×btc_ret за то же окно.**
2. **Кластеризация по дням.** До 26–29 сигналов в один общерыночный день — одна ставка, посчитанная
   26 раз. **Всегда схлопывать в календарные дни, равный вес дню.**
3. **Винрейт = 1/(1+R)** — чистая геометрия брекета. Тейк 0.67 ATR даёт ровно 60.0% побед и −8.9 б.п.
   Чем выше винрейт, тем хуже результат.

⚠️ Гэповых паттернов в крипте не существует: `abandoned baby`, runaway gap → **0 событий на 345k
баров**, потому что крипта торгуется 24/7.

Наука (Marshall/Young/Rose, Lu et al., Bajgrowicz&Scaillet, Ho et al.) говорит то же самое.

## Что осталось живого

**`dip_bounce_v1`** — идея владельца «купить просадку ≥10%, ждать +3%». Трекер на 130 монетах
(топ по обороту, заморожен), кроны `dip-klines-refresh` (*/15) и `dip-bounce-scan` (:47), 🎯-алерты.
Статус: **ранен**. Day-clustered +0.487%/день t=1.66, но **бета 1.45 → hedged +0.179%/день t=0.84**.
Гейт ужесточён: promote требует ПОЛОЖИТЕЛЬНОЙ хеджированной альфы. Развязка ~конец августа.

**Убиты аудитом:** `funding_squeeze_v1` (кластеризация, соло-дни −0.68%), `vwap_dip_v1` (бета 1.19).
Базис — пусто (45 дней данных, монотонность сломана).

## Единственный незакрытый измеримый вопрос

**Мейкерское исполнение.** Кост 10 б.п. → ~4 б.п. Эджа не создаёт, но это единственная переменная
во власти владельца. Замер: доля исполнения лимиток, неблагоприятный отбор, реальный кост. Неделя.

## Инфраструктура

- ⚠️ **Railway деплоит с ветки `main` репозитория TradingBot.** Значит ЛЮБОЙ коммит в `main` —
  это автовыкатка на сервис с боевыми ключами Binance, включая правку комментария или доки.
  Работаем в `feature/testnet-risk-core`; в `main` не коммитить и не пушить.
  - Источник этих фактов — **Railway API, опрошен 11.08.2026**, а не репозиторий: в дереве нет ни
    `railway.json`, ни `Procfile`, ни `.github`, так что по коду это не проверяется в принципе.
    Что именно отдал API: воркспейс один, проект `agile-miracle`, сервис `TradingBot`, деплой
    `dfd2668d` от 28.07.2026 в статусе SUCCESS, коммит `9739f5c`, источник github `TradingBot`/`main`,
    1 реплика, публичных доменов нет. Значений переменных никто не читал — только имена.
- **Supabase** `ptlxojilgqylzxshsrlm`. Известные кроны: `dip-klines-refresh` (*/15),
  `dip-bounce-scan` (:47), `lab-forward-run` (*/6ч, починен 10.08 — до этого форвард полтора месяца
  мерил пустоту), `long-v2-scan` (:41), `live_time_closer` (нигде не задокументирован, встречается
  только в комментариях `SupabaseSignalBridge.java:135` и `:303` на ветке `main`).
  **Полный список неизвестен** — кронов больше, чем записано. Первый читающий запрос после подъёма
  базы: `select jobname, schedule, active from cron.job;`
- **n8n**: 5 активных воркфлоу в одном личном проекте. Крипто-относящихся три: `lab-watchdog`
  (каждые 30 мин; внешний сторож, анти-спам: тревога при смене состояния и далее не чаще раза в 4ч),
  `execution-cost-probe` (каждые 15 мин), `crypto-digest` (каждые 12ч, 10:00 и 22:00 UTC).
  Остальные два — свадебный стек на ДРУГОЙ базе Supabase, к крипте отношения не имеют, не трогать.
  - ⚠️ Часовой пояс инстанса **Europe/Warsaw (UTC+2)**: cron-выражения в UI не совпадают с UTC.
    Это ловушка при чтении расписаний — пересчитывай.
  - `execution-cost-probe` объявлен и как pg_cron в Supabase, и как воркфлоу n8n. Кто из двух
    настоящий планировщик — по репозиторию не определить: **не проверено, база лежит**.
  - `crypto-strategy-brain` через MCP не виден. Это не доказывает ни архивацию, ни удаление:
    у `search_workflows` нет параметра «включая архивные». Статус неизвестен.
- **Запуск бота**: `Start-Process` с `JAVA_HOME=C:\Users\Asus_F15\.jdks\ms-21.0.9`,
  аргумент **в кавычках**: `'"--args=--source supabase"'` — иначе разъедется по пробелу.
  Логи java.util.logging идут в **stderr** (`bot-run.err.log`).
- ⚠️ `gradlew run` оставляет дочернюю JVM живой — проверять и убивать вручную.

## Деньги — честно

**Эджа нет.** Направленная торговля по графику на публичных данных — угадывание, это измерено.
Реальные деньги в этом стеке не оправданы. Плечо при нулевом эдже не помогает, а превращает
медленную утечку в один выстрел: худшая сделка выборки −32.7%, при 3x это ликвидация.

Что не является угадыванием: спот-DCA на BTC/ETH через цикл; сбор фандинга в эйфории (сейчас ~1 б.п.);
снижение издержек через мейкера. Ни одно из трёх не похоже на то, что продают в видео.

Основной денежный трек владельца — n8n-фриланс.

## Чего НЕ делать

- не коммитить и не пушить в `main` — это автовыкатка на боевой сервис;
- не трогать `BINANCE_USE_TESTNET` в Railway (см. третий красный раздел);
- не заводить реальные деньги без пройденного гейта;
- не переоткрывать поиск по свечам/уровням/паттернам — множество пройдено целиком;
- не давать субагентам создавать таблицы в продакшн-базе;
- не считать per-event t на событийных стратегиях — только day-clustered и только с хеджем;
- не удалять данные лаборатории — их физически нельзя собрать заново;
- не просить и не принимать API-ключи в переписку.
