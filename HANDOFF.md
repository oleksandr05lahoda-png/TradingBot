# Хэндоф — состояние на 10.08.2026

Вставь это в новый чат целиком.

---

Продолжаем работу над `C:\Users\Asus_F15\IdeaProjects\TradingTelegramBot`, ветка
`feature/testnet-risk-core` (main НЕ трогать до ревью). Прочитай `README.md` и `CLEANUP.md` в корне.

## Что построено

Фундамент под крипто-фьючерсы: **риск-движок** (сколько брать и можно ли вообще) + **слой
исполнения** для Binance USDⓈ-M **testnet**. Стратегии внутри НЕТ и быть не должно — ни индикаторов,
ни генерации сигналов. Старый стек (8 классов, 13 628 строк) удалён. Сейчас 42 файла, ~4 000 строк
кода, **193 теста**, точка входа `com.bot.app.TestnetBot`.

Рамки зашиты структурно, не комментариями: боевого URL нет в коде и тест роняет сборку при его
появлении; `MAX_LEVERAGE = 5` и потолок риска 1% — константы, которые не поднять переменной
окружения; позиция без стопа не конструируется; `SignalSource` имеет ровно две реализации и тест
это считает.

## Что уже проверено живой биржей (10.08)

Полная цепочка прошла: `n8n → Supabase(bot_orders) → бот → demo-fapi → стоп + 2 тейка → закрытие
по команде → обратно в БД`.

Живой прогон нашёл 4 дефекта, невидимых для тестов — все починены:
1. **Binance с декабря 2025 перевёл условные ордера на `POST /fapi/v1/algoOrder`** (старый эндпоинт
   отвечает `-4120`). Поля: `algoType=CONDITIONAL`, `type` (не orderType), `clientAlgoId`,
   `triggerPrice`. Справочник New Order при этом всё ещё врёт, что STOP_MARKET там валиден.
2. Исключение из reconcile убивало процесс — при открытой позиции.
3. Стартовое расхождение тоже завершало процесс → позицию нельзя было закрыть через бота.
4. Reduce-only закрытие возвращало цену 0 и портило замер.

Защита сработала трижды: при отказе стопа позиция закрывалась reduce-only за секунду, торговля
останавливалась. Голой позиции не было ни разу.

## Проверка стопа — ЗАКРЫТА (10.08, кодом и тестами; живой сделкой ещё нет)

`demo-fapi` по-прежнему не отдаёт список условных ордеров, но отвечает на запрос по имени. Замерено
прямо по эндпоинту, не по документации:

| запрос | ответ |
|---|---|
| `GET /fapi/v1/algoOpenOrders?symbol=…` | `404 -5000 Path … is invalid` — списка нет |
| `GET /fapi/v1/algoOrder?clientAlgoId=<несуществующий>` | `400 -2013 Order does not exist` |

`-2013` — это `BinanceErrorCodes.NO_SUCH_ORDER`, то есть «нет ордера» отличимо от «эндпоинт сломан».
На этом и построено: `ExposureBook.OpenPosition` носит `protectiveStopId`, `RiskEngine.registerFill`
требует его (позицию нельзя записать, не назвав её стоп), Reconciler переносит id между проходами и
спрашивает биржу поимённо. Стоп рабочий → сходится и заодно проверяется буфер ликвидации; отменён,
истёк или неизвестен бирже → `POSITION_WITHOUT_STOP` и стоп торговли.

Три случая молчат намеренно, потому что алерт на здоровой позиции приучает игнорировать все алерты:
id нет в записи (позиция подхвачена с биржи), запрос не прошёл (сеть), статус в незнакомых словах.
Сработавший стоп получает **ровно один** проход форы — обычный стоп-аут не должен поднимать тревогу,
но триггер, за которым не пришло закрытие, через проход становится дефектом.

**Что осталось:** подтвердить живой сделкой, что рабочий стоп на demo читается по имени и парсится
(шейп ответа GET на алго-ордер вживую ещё не видели — на пустом счёте его не на чем проверить).
До этого бота на ночь без присмотра не оставлять.

Дальше по плану: включить зонд на расписание → ~100 замеров стоимости за двое суток → пересчитать
гейты лаборатории на измеренной цифре вместо выдуманной.

## Инфраструктура

- **Supabase** `ptlxojilgqylzxshsrlm`. Очередь `bot_orders`: `pending→sent→rejected` и
  `close_requested→close_sent→closed`; `filled_price/filled_qty` = вход, `close_price/close_qty` =
  выход. RPC `probe_tick(symbol, price)`, вью `v_execution_cost`.
- **n8n** workflow `execution-cost-probe` (`aHRcu1IFVyTVstIj`) — **ВЫКЛЮЧЕН**, включать только с
  запущенным ботом.
- **Запуск**: `JAVA_HOME=/c/Users/Asus_F15/.jdks/ms-21.0.9 ./gradlew run --args="--source supabase"`.
  JAVA_HOME в системе сломан — задавать в команде. Ключи в `local.env` (в .gitignore).
- ⚠️ `gradlew run` оставляет дочернюю JVM живой после остановки задачи — проверять и убивать
  вручную, иначе два бота разбирают одну очередь.

## Деньги — честно

**Стратегии нет.** Форвард: **0 из 24** слив прошли. Гипотеза H-FRESH-1 (жирный funding свежих
перпов) на форварде инвертировалась: −4.5% годовых против +15.6% на dev. Реальные деньги — только
после того, как что-то пройдёт свой предрегистрированный гейт. Не изображать оптимизм.

Первая измеренная цифра: круг вход-выход ≈ **10 б.п.** ($1.29 на $1243.87). Гейты лабы закладывают
20 и 40 б.п.

## Чего НЕ делать

- не заводить реальные деньги;
- не удалять данные лаборатории в Supabase — их физически нельзя собрать заново;
- не добавлять третью реализацию `SignalSource` (это была бы стратегия в маскировке);
- не разводить длинные комментарии — их уже дважды просили сократить, сейчас 17%;
- не просить и не принимать API-ключи в переписку.

## Незакрытое по безопасности

- Supabase secret key засветился в скриншоте 10.08 — проверить у пользователя, отозван ли.
- RLS выключен на 12 таблицах, включая `paper_signals`. Чинить только разобравшись, каким ключом
  ходят n8n-воркфлоу, иначе сломается сбор данных.
