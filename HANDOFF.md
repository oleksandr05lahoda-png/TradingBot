# Хэндоф — состояние на 11.08.2026

Вставь это в новый чат целиком.

---

Продолжаем работу над `C:\Users\Asus_F15\IdeaProjects\TradingTelegramBot`, ветка
`feature/testnet-risk-core` (main НЕ трогать до ревью).

## 🔴 ПЕРВЫМ ДЕЛОМ: база лежит, лечится только владельцем

Пять параллельных агентов создали **372 МБ временных таблиц** в продакшн-базе размером 1415 МБ.
Диск переполнился, Postgres в петле `recovery mode → not accepting connections`. **Само не
вылечится** — для завершения восстановления тоже нужно место. Management API врёт `ACTIVE_HEALTHY`.

**Лечение:** Supabase → `crypto-listings` → Settings → Database → +2 ГБ. Потом:
```sql
drop table if exists zz_sc_base, zz_sc_bars, _tmp_pavol_bars, _tmp_pavol_hl, _tmp_pavol_sig, lab3c_p, lab3c_bars;
```

Пока база лежит: бот не работает, все кроны стоят, форвард dip_bounce не набирает сделки,
боевой контур на Railway тоже не читает очередь.

**Урок на будущее:** субагентам запрещать `CREATE TABLE` в проде — только SELECT и CTE.

## 🔴 Второе: боевой путь снаружи проекта жив

Наш бот testnet-only, но старый контур никто не выключал:
`sleeve_gates.long_v2.live = true` → крон `long-v2-scan` (:41) → `enqueue_live_order` (цена с
боевого `fapi.binance.com`) → строка `testnet=false` → сервис **TradingBot на Railway** исполняет
боевыми ключами. Уже стрелял: `bot_orders` id 1, реальный XRPUSDT $5.17 от 08.07.
Его единственная форвардная сделка: **−15.40%**.

Снять (решение владельца, само не делать):
`update sleeve_gates set criteria = jsonb_set(criteria,'{live}','false') where sleeve='long_v2';`

## Что построено

Риск-движок + слой исполнения для Binance USDⓈ-M **testnet**. Стратегии внутри нет и быть не
должно. 42 файла, **193 теста**, точка входа `com.bot.app.TestnetBot`.

Рамки зашиты структурно: боевого URL нет и тест роняет сборку при его появлении; `MAX_LEVERAGE = 5`
и потолок риска 1% — константы; позиция без стопа не конструируется; `SignalSource` имеет ровно две
реализации и тест это считает.

**Проверено живой биржей:** цепочка `n8n → Supabase → бот → demo-fapi → стоп + 2 тейка → закрытие`.
Проверка стопа поимённым запросом закрыта и подтверждена живой сделкой (`algoStatus:"NEW"` читается,
снятие стопа даёт `POSITION_WITHOUT_STOP` + halt).

**Стоимость, два замера:** ~10 б.п. за круг (8 б.п. комиссии + 2.4 б.п. проскальзывание).

## ⭐ ГЛАВНОЕ: price action закрыт окончательно

**~50 конструкций, 700 000+ симулированных сделок, НОЛЬ с положительной хеджированной альфой.**

Закрыты: все свечные паттерны (молот t=−3.64, поглощения t=−4.96, доджи t=−3.76, пронзающая
t=−4.12, звёзды, пинцеты, трёхбарные, сжимающиеся), уровни, пробои, ложные пробои (t=−12.5),
диапазоны (t=−12.7), VWAP-полосы, MACD, структура, отбор лучших, обе стороны, три таймфрейма.

**Вскрыт механизм иллюзии — три независимых способа выглядеть прибыльным при нулевом эдже:**

1. **Бета BTC 1.1–1.9 на каждой строке.** Любое «покупай слабость на альтах» = плечевой лонг BTC.
   Убило vwap_dip (сырое t=4.81 → hedged t=1.14) и на 2/3 dip_bounce.
   **Всегда считать hedged = day_ret − beta×btc_ret за то же окно.**
2. **Кластеризация по дням.** До 26–29 сигналов в один общерыночный день — одна ставка, посчитанная
   26 раз. **Всегда схлопывать в календарные дни, равный вес дню.**
3. **Винрейт = 1/(1+R)** — чистая геометрия брекета. Тейк 0.67 ATR даёт ровно 60.0% побед и −8.9 б.п.
   Чем выше винрейт, тем хуже результат.

⚠️ Гэповых паттернов в крипте не существует: `abandoned baby`, runaway gap → **0 событий на 345k
баров**, потому что крипта торгуется 24/7.

Наука (Marshall/Young/Rose, Lu et al., Bajgrowicz&Scaillet, Ho et al.) говорит то же самое.

## Что осталось живого

**`dip_bounce_v1`** — идея владельца «купить просадку ≥10%, ждать +3%». Трекер на 130 монетах
(топ по обороту, заморожен), кроны `dip-klines-refresh` (*/15) и `dip-bounce-scan` (:47), 🎯-алерты.
Статус: **ранен**. Day-clustered +0.487%/день t=1.66, но **бета 1.45 → hedged +0.179%/день t=0.84**.
Гейт ужесточён: promote требует ПОЛОЖИТЕЛЬНОЙ хеджированной альфы. Развязка ~конец августа.

**Убиты аудитом:** `funding_squeeze_v1` (кластеризация, соло-дни −0.68%), `vwap_dip_v1` (бета 1.19).
Базис — пусто (45 дней данных, монотонность сломана).

## Единственный незакрытый измеримый вопрос

**Мейкерское исполнение.** Кост 10 б.п. → ~4 б.п. Эджа не создаёт, но это единственная переменная
во власти владельца. Замер: доля исполнения лимиток, неблагоприятный отбор, реальный кост. Неделя.

## Инфраструктура

- **Supabase** `ptlxojilgqylzxshsrlm`. Кроны: `dip-klines-refresh`, `dip-bounce-scan`,
  `lab-forward-run` (*/6ч, починен 10.08 — до этого форвард полтора месяца мерил пустоту),
  `execution-cost-probe` и старые.
- **n8n**: `execution-cost-probe`, `crypto-digest`, `lab-watchdog` (внешний сторож, анти-спам:
  тревога при смене состояния и далее не чаще раза в 4ч). `crypto-strategy-brain` заархивирован —
  он генерировал кандидатов из мёртвых семейств каждые 2 часа.
- **Запуск бота**: `Start-Process` с `JAVA_HOME=C:\Users\Asus_F15\.jdks\ms-21.0.9`,
  аргумент **в кавычках**: `'"--args=--source supabase"'` — иначе разъедется по пробелу.
  Логи java.util.logging идут в **stderr** (`bot-run.err.log`).
- ⚠️ `gradlew run` оставляет дочернюю JVM живой — проверять и убивать вручную.

## Деньги — честно

**Эджа нет.** Направленная торговля по графику на публичных данных — угадывание, это измерено.
Реальные деньги в этом стеке не оправданы. Плечо при нулевом эдже не помогает, а превращает
медленную утечку в один выстрел: худшая сделка выборки −32.7%, при 3x это ликвидация.

Что не является угадыванием: спот-DCA на BTC/ETH через цикл; сбор фандинга в эйфории (сейчас ~1 б.п.);
снижение издержек через мейкера. Ни одно из трёх не похоже на то, что продают в видео.

Основной денежный трек владельца — n8n-фриланс.

## Чего НЕ делать

- не заводить реальные деньги без пройденного гейта;
- не переоткрывать поиск по свечам/уровням/паттернам — множество пройдено целиком;
- не давать субагентам создавать таблицы в продакшн-базе;
- не считать per-event t на событийных стратегиях — только day-clustered и только с хеджем;
- не удалять данные лаборатории — их физически нельзя собрать заново;
- не просить и не принимать API-ключи в переписку.
