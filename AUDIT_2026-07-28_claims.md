# Аудит заявленных свойств (project_state id=26)

Дата: 2026-07-28. Ветка `refactor/cleanup-2026-07`.

Метод: 8 параллельных агентов, по одному на файл; каждое утверждение проверялось 
фактическим вызовом, затем 6 подтверждённых safety-утверждений атаковались скептиками.


**Итог: 266 утверждений, 197 ложных или устаревших (74%). Из 6 атакованных «подтверждённых» safety-утверждений 3 не выдержали атаки.**


## Не выдержали состязательной проверки

Аудитор счёл их подтверждёнными, скептик опроверг.


| Файл:строка | Утверждение | Почему ложно |
|---|---|---|
| `SupabaseSignalBridge:159` | "TESTNET-LOCKED IN CODE: refuses to OPEN any position unless the executor reports the testnet/demo endpoint AND BINANCE_USE_TESTNET was set explicitly (absence is a refusal, never  | REFUTED. The gate's *plumbing* is fine — I confirmed executionAllowed() is the only thing standing between poll() and drainOpens() (:138 / :154), TESTNET_ENV/TESTNET_SET are genuinely unparsed (:66-67), BinanceTradeExecutor.useTestnet is `private final`, assigned once at :190 with no setter, so bridge field `useTestnet` (:104) cannot go stale and there is no check/use race. openPositionWithSl is called from exactly one place in the repo (SupabaseSignalBridge:274). So no bypass exists *through drainOpens*.  The claim fails on the other half of the same doc block: "refuses to OPEN **any** positi |
| `SupabaseSignalBridge:47` | "BUDGETED: defers any pending beyond BRIDGE_MAX_OPEN concurrent; deferred rows stay 'pending' and are retried, or expire via MAX_PENDING_AGE_MIN" | REFUTED. The auditor verified the deferral mechanics (correct) but never verified that countActive() actually measures live exposure. Two independent defects break the cap.  (1) FAIL-OPEN COUNT. countActive() (:317-320) calls sbGet(), which on any non-2xx logs a warning and returns an empty JSONArray (:378-381). A failed count read therefore yields active=0 and drainOpens() opens up to the pending query's limit=20 with no cap in force. It is a separate HTTP request from the pending fetch (:245), so one failing while the other succeeds is ordinary (transient 5xx/429/pooler blip). The class expl |
| `SupabaseSignalBridge:124` | poll() asymmetry: "Everything that can only SHRINK exposure runs unconditionally ... Only drainOpens(), the single path that can create exposure, sits behind executionAllowed()" an | REFUTED. The ordering half is true (SupabaseSignalBridge.java:150 reconcileOpen precedes :155 drainOpens; drainOpens is the only call after the gate at :154; poll() is the live root via BotMain.java:414-417), but the load-bearing safety half — "everything ungated can only SHRINK exposure / drainOpens is the single path that can create exposure" — is false. The ungated drainCloses() (:151, :312) calls BinanceTradeExecutor.closePosition() (:1338), which sends sendMarketOrder(symbol, !wasLong, absQty, cid, reduceOnly=true) at :1368. On a -4061 hedge-mode reject, the fallback at :1697-1719 re-POST |

## Выдержали атаку

- `SupabaseSignalBridge:28` — "LONG-ONLY: rejects any row whose side != LONG (DB also enforces it — constraint bot_orders_side_long CHECK (side = 'LONG'))"
- `SupabaseSignalBridge:68` — "BRIDGE_ALLOW_REAL is still read but grants nothing" / "Read, but no longer grants anything ... Kept so the log can say it was ignored"
- `SupabaseSignalBridge:86` — "Today RiskGuard constrains nothing: it is instantiated in BotMain and never called"

## Ложные и устаревшие — уровень БЕЗОПАСНОСТИ (66)

| Файл:строка | Вид | Вердикт | Утверждение | Проверка |
|---|---|---|---|---|
| `BinanceTradeExecutor:21` | javadoc | **false** | «этот класс ничего не делает без явного вызова из BotMain. Сам по себе он лежит "в спячке". Активация — на Этапе 3.» | static INSTANCE is built at class-init (:186) and the ctor at :255-259 POSTs /fapi/v1/positionSide/dual — an account-mode MUTATION — whenever keys are present and !useTestnet, before any caller does anything. The live driver is not BotMain: SupabaseSignalBridge.java:98 holds getInstance() as a field and :274/:312 call openPositionWithSl/closePosition. 'Этап 3' (BotMain.Dispatcher) was deleted in v88.2 (BotMain.java:4 |
| `BinanceTradeExecutor:40` | javadoc | **stale** | «NEVER OPEN WITHOUT SL … Если SL не подтвердился за 5 секунд — позиция закрывается принудительно.» | SL_PLACEMENT_TIMEOUT_MS default is 2000 not 5000 (:229), and slPlacementTimeoutMs is never read: decl :117, assign :229, a javadoc @link :625 and a stale comment :1161 are the only occurrences (grep). The real logic (:1169-1193) is attempt-count based — 4 attempts, each an HTTP call with .timeout(10s) plus 300-500 ms sleeps — so the naked window before the emergency close at :1196 can reach ~40 s, not 5 s. |
| `BinanceTradeExecutor:52` | javadoc | **false** | «Настоящий trade-gate = LIVE_TRADING_ARMED=0 (без него ордеров НЕТ даже на real, бот read-only)» | LIVE_TRADING_ARMED is read only at BotMain.java:122 and :478, and BotMain.java:479-483 states it now gates ONLY the deleted candle path. The live order path BotMain.java:414-417 -> SupabaseSignalBridge.poll() -> drainOpens()/drainCloses() never reads it. SupabaseSignalBridge.java:149-155 runs drainCloses() UNGATED on any endpoint, so with LIVE_TRADING_ARMED=0 a close_requested row still sends a real MARKET order (Sup |
| `BinanceTradeExecutor:57` | javadoc | **false** | «ISOLATED MARGIN, FIXED LEVERAGE. Перед первой сделкой по символу выставляются margin type = ISOLATED … leverage = LEVERAGE (default 5x)» | :674-678 SKIPS initSymbolMargin/initSymbolLeverage entirely when useTestnet and pre-adds the symbol to initializedSymbols so the :679 block never runs. openPositionWithSl's only caller refuses to open unless the executor is on testnet (SupabaseSignalBridge.java:176-180), so every position that can be opened today is opened with neither ISOLATED nor the 5x leverage ever sent to the exchange. |
| `BinanceTradeExecutor:86` | comment | **false** | «Each LIVE order carries a deterministic newClientOrderId so that a retried order after an AMBIGUOUS timeout cannot double-fill.» | newClientOrderId (:103-109) is System.nanoTime() + AtomicLong — a NEW id every call, not deterministic across restarts. Only the three MARKET paths pass one (:898, :969, :1367, :2421 -> body :1654). The SL and TP ALGO orders carry no newClientOrderId at all (bodies :1900-1912, :1975-1988), and the SL is re-POSTed up to 4x at :1170-1193, so an accepted-but-timed-out SL can be duplicated. |
| `BinanceTradeExecutor:93` | comment | **false** | «DORMANT in paper (LIVE_TRADING_ARMED=0 → these methods are never called)» | openPositionWithSl's sole caller SupabaseSignalBridge.java:274 is gated on SUPABASE_BRIDGE_ENABLED + BINANCE_USE_TESTNET (SupabaseSignalBridge.java:117-119, :167-182) and never on LIVE_TRADING_ARMED; with LIVE_TRADING_ARMED=0 and the bridge on, entry/close/emergency order methods are all live. |
| `BinanceTradeExecutor:240` | logbanner | **false** | boot banner «[Executor] init: %s leverage=%dx risk=%.1f%%/trade keys=%s» | riskPctPerTrade is only the sizing seed (:775-776). EXEC_MAX_NOTIONAL_USD (default 6.0, :838-842) truncates notional AFTER sizing and after the 20% cap, so realized risk per trade is ~$6 x slDistPct regardless of the printed percentage: on a $1000 balance with a 2% stop, the advertised 'risk=2.0%' ($20) is actually $0.12. BotMain.java:501-505 republishes the same number to Telegram. |
| `BinanceTradeExecutor:272` | javadoc | **false** | «Auto-handles -4067 … by enumerating all open orders, cancelling them, then retrying the position-side switch once.» | :304-308 (v86.27) only logs a warning — 'leaving account mode unchanged, NOT cancelling orders'; there is no enumeration, no cancel and no retry. The afterCancel parameter (:280) is now vestigial: the sole call site :277 passes false and nothing ever passes true. |
| `BinanceTradeExecutor:317` | javadoc | **stale** | «Used by ensureOneWayMode to clear blockers preventing position-mode change, and exposed publicly for callers (PositionTracker reconcile) … Returns count of orders cancelled.» | ensureOneWayMode no longer calls it (:304-308); PositionTracker does not exist in the repo (grep: comment references only). The single real caller is the -4067 branch of initSymbolMargin (:1521-1523), i.e. an account-wide wipe of regular AND algo (SL/TP) orders now fires from inside the trade-open path (:680) — the exact thing :306-308 says must not happen on a real account. The return value is also not a count: :348 |
| `BinanceTradeExecutor:752` | comment | **false** | «он ловит ОПАСНЫЙ дрейф плеча: при 10x liqDist≈8% < широкий SL → слив ДО стопа» | the guard computes liqDistFrac from the local env-derived `leverage` field (:212, :756), never from the exchange. On the only path that can open a position today (testnet — SupabaseSignalBridge.java:176-180) initSymbolLeverage is skipped (:674-678), so the account's real leverage is whatever the demo UI holds; a drift between env LEVERAGE and the account cannot be detected by this check. |
| `BinanceTradeExecutor:1112` | comment | **stale** | «(c) SL distance bloated >1.7× planned → abort, risk budget blown» / «Risk-budget guard: SL distance after slippage must not exceed 1.7× the planned distance» | the v86.19 anchoring at :1061-1064 sets effStop = idea.stop + (actualEntry − idea.price), so actualSlDist (:1117) = \|actualEntry − effStop\| ≡ \|idea.price − idea.stop\| = plannedSlDist (:1116) exactly whenever idea.price > 0 — always true on the only live path (SupabaseSignalBridge.java:325 sets entry from the queue row, and the DB rejects non-positive entries). The >1.7× branch at :1118 is arithmetically unreachab |
| `BinanceTradeExecutor:1161` | comment | **false** | «4 attempts × ~250ms each fits within slPlacementTimeoutMs (2s default).» | each attempt calls sendStopMarketOrderDiag whose request has .timeout(Duration.ofSeconds(10)) (:1919) and the loop sleeps 300-500 ms between tries (:1192); worst case is ~42 s, ~20x the stated bound. The v86.24 comment immediately above (:1164-1166) itself records that the wall-clock deadline was REMOVED, contradicting this line. |
| `BinanceTradeExecutor:1334` | javadoc | **false** | «Force-close … Used by RiskGuard when kill-switch fires, by TimeStop, or by emergency procedures.» | closePosition's only caller in the repo is SupabaseSignalBridge.java:312 (drainCloses). RiskGuard.java contains no reference to BinanceTradeExecutor or closePosition (grep 'Executor\|closePosition\|canTrade\|TimeStop' returns only its own doc lines), and BotMain.java:452-459 records that RiskGuard is instantiated and never called. No TimeStop class exists. |
| `BinanceTradeExecutor:1364` | comment | **false** | «closePosition has no pre-flat check, so if a caller-level retry re-invokes after an ambiguous timeout, the SAME id makes Binance refuse the resend with -4015 → treated as SUCCESS → no double-fill.» | wrong on both halves: closePosition DOES pre-check flat at :1346-1355 (returns early when \|posQty\| < 1e-12), and :1367 mints a FRESH id via newClientOrderId (:103-109, nanoTime + incrementing seq) on every invocation — a caller-level retry therefore sends a DIFFERENT newClientOrderId, Binance cannot dedupe it, and the -4015 success path (:1673) never fires across invocations. The claimed cross-call idempotency does |
| `BotMain:13` | javadoc | **false** | "Mode: PURE SIGNAL SCANNER. No auto-trade, no order execution." | BotMain.main schedules SupabaseSignalBridge.poll (BotMain.java:414-417) → poll() → drainOpens() → executor.openPositionWithSl(idea, bal) (SupabaseSignalBridge.java:155,274) → real MARKET order in BinanceTradeExecutor.openPositionWithSl. Order execution is the bot's only remaining function since v88.2 ("executor-only bot"). |
| `BotMain:56` | javadoc | **stale** | audit map: "OBSERVATION_MODE… напрямую проверяется в Dispatcher.dispatch перед каждой отправкой в Telegram" | There is no Dispatcher class or dispatch() method left in the tree — only comment references (BotMain.java:456 says the Dispatcher was deleted in v88.2). OBSERVATION_MODE now only selects a boot banner (BotMain.java:449,478,489,507,519). |
| `BotMain:97` | comment | **false** | OBSERVATION_MODE: "signals reach Telegram tagged 🧪 [PAPER]. Calibrator still records outcomes, so the bot learns without budget risk." | git grep for "PAPER" and "🧪" over *.java returns only BotMain's own comment plus the unrelated "🧪 TESTNET" boot label (BotMain.java:504) — no tagging code exists. The calibrator was removed entirely (DecisionEngineMerged.java:221 "ProbabilityCalibrator removed. Its write path was dead"), so nothing records outcomes. |
| `BotMain:105` | comment | **false** | AUTO_TRADE_ENABLED: "Even when ON, it goes through RiskGuard.canTrade() first — daily loss limit, BTC crash, trade limits all block automatically." | grep over src/main/java/com/bot: canTrade() is defined at RiskGuard.java:206 and called from nowhere; recordTradeOpened/recordTradeClosed/updateBtcPrice (RiskGuard.java:297/311/325) likewise have zero call sites. The only RiskGuard touch on any live root is BotMain.java:461 getInstance()+statusLine(). The file itself contradicts this comment at BotMain.java:452-459. |
| `BotMain:107` | comment | **false** | AUTO_TRADE_ENABLED: "Default starts on TESTNET (BINANCE_USE_TESTNET=1). To use real money you must explicitly set BINANCE_USE_TESTNET=0 with valid real API keys." | Inverted. BinanceTradeExecutor.java:190 `useTestnet = "1".equals(getenv().getOrDefault("BINANCE_USE_TESTNET","0"))` — an ABSENT variable selects REAL, and BinanceTradeExecutor.java:208-209 then sets baseUrl=https://fapi.binance.com with BINANCE_API_KEY/SECRET. Real money is the default endpoint, not testnet; only SupabaseSignalBridge.executionAllowed() (SupabaseSignalBridge.java:167-182) refuses opens in that state. |
| `BotMain:118` | comment | **stale** | LIVE_TRADING_ARMED: "HARD live-trading kill switch — default OFF (DISARMED)... NO real trades are executed. Sits ON TOP of BOT_AUTO_TRADE / OBSERVATION_MODE." | LIVE_TRADING_ARMED is read at exactly one place, BotMain.java:478, to pick which boot log/Telegram banner is printed. The live order path (SupabaseSignalBridge.poll→drainOpens, SupabaseSignalBridge.java:136-156,243-293) never reads it. BotMain.java:479-483 admits the flag now gates only the deleted candle path. |
| `BotMain:164` | comment | **false** | MAX_SIGNALS_PER_CYCLE: "ISC + RiskGuard.MAX_CONCURRENT_POSITIONS remain authoritative limiters on actual trading." | No ISC class exists anywhere in src/main/java/com/bot (only two stale mentions in DecisionEngineMerged comments); RiskGuard.MAX_CONCURRENT_POSITIONS is only ever formatted into RiskGuard.statusLine() (RiskGuard.java:474) because canTrade() has no callers. MAX_SIGNALS_PER_CYCLE itself (BotMain.java:166) is read by nothing. |
| `BotMain:418` | logbanner | **false** | boot log reports the endpoint: "SupabaseSignalBridge ENABLED — draining bot_orders (testnet=" + getenv BINANCE_USE_TESTNET, default "0" | This is a second, differently-parsed read of the endpoint variable: the executor decides with `"1".equals(...)` (BinanceTradeExecutor.java:190), the banner prints the raw string. BINANCE_USE_TESTNET=true (or "yes"/"TRUE") makes the banner say testnet=true while the executor is built against https://fapi.binance.com. SupabaseSignalBridge.java:99-104 explicitly forbids this pattern ("never re-read from env here") — Bot |
| `BotMain:467` | comment | **false** | "Initialize the executor. It is a no-op until BOT_AUTO_TRADE=1 AND OBSERVATION_MODE=0." | SupabaseSignalBridge (scheduled 50 lines earlier at BotMain.java:414-417) holds `BinanceTradeExecutor.getInstance()` (SupabaseSignalBridge.java:98) and calls openPositionWithSl (line 274) / closePosition (line 312). Neither the bridge nor the executor reads BOT_AUTO_TRADE or OBSERVATION_MODE — grep shows AUTO_TRADE_ENABLED and OBSERVATION_MODE are referenced only inside BotMain's boot-banner if/else. |
| `BotMain:502` | telegram | **false** | Telegram: "Единственное ограничение на живом пути — не более %d открытых позиций из очереди." | BRIDGE_MAX_OPEN is not the only live-path constraint: SupabaseSignalBridge enforces the testnet lock (executionAllowed, line 167-182), LONG-only (line 251), stale-pending expiry (line 255) ; BinanceTradeExecutor enforces max spread (line 700-704), SL-distance sanity 0.10–10% (line 719-729), the liquidation-distance guard (line 755-766) and EXEC_MAX_NOTIONAL_PCT. The RiskGuard half of the same message ("риск-лимиты НЕ |
| `BotMain:508` | logbanner | **false** | boot log: "BOT_AUTO_TRADE=1 but OBSERVATION_MODE=1 — paper wins, no live trades." | OBSERVATION_MODE is never consulted on the order path; with SUPABASE_BRIDGE_ENABLED=1 the bridge keeps draining bot_orders and opening positions (SupabaseSignalBridge.java:136-156,243-293) while this line asserts paper mode. |
| `BotMain:517` | logbanner | **false** | boot log: "Auto-trade disabled (BOT_AUTO_TRADE=0 OBSERVATION_MODE=…)" | Reached whenever BOT_AUTO_TRADE=0, but the bridge scheduled at BotMain.java:414 opens real orders independently of that flag (SupabaseSignalBridge.java:243-293). The operator reads "auto-trade disabled" while automated trading is live. |
| `DecisionEngineMerged:405` | javadoc | **false** | "Blocks re-entry in same direction for POST_EXIT_COOLDOWN_MS to prevent spin-trading." (markPostExitCooldown javadoc) | postExitCooldown is WRITE-ONLY: put() at DecisionEngineMerged.java:387 (recordLoss) and :408 (markPostExitCooldown); repo-wide grep for `postExitCooldown` returns no read/get/containsKey anywhere. POST_EXIT_COOLDOWN_MS (line 260) is referenced by nothing. Live caller SignalSender.java:1596 fills the map after every UDS close; nothing consults it, and the only order-opening path (SupabaseSignalBridge.drainOpens -> bui |
| `DecisionEngineMerged:644` | comment | **stale** | "[HOLE-1 FIX] SignalSender computes ALL modifiers (category, flag-based, session, ISC, small-balance) when building Telegram display, then stores the resulting ratio here. Executor reads it and applie | Executor half CONFIRMED: BinanceTradeExecutor.java:781-786 reads getExecutorSizeMultiplier() and multiplies qty. Producer half is dead: the only SignalSender call to setExecutorSizeMultiplier is SignalSender.java:3388 inside rebuildIdea(), and rebuildIdea has zero callers (grep `rebuildIdea` = declaration + its own comments only). The live value comes from the Supabase row field `size_mult` (SupabaseSignalBridge.java |
| `DecisionEngineMerged:1018` | telegram | **stale** | Telegram card line "⏳ Time-stop: %d мин" (480 on 1h) — commented as "синхрон с PT_TIME_STOP_MS / ISC / BT", "the real position time-stop (PositionTracker PT_TIME_STOP_MS) is 480 мин" | PositionTracker.java was deleted in v88.2 (git show f7e663d: `delete ... PositionTracker.java 956 lines`); no class ISC/InstitutionalSignalCore exists (also deleted); grep PT_TIME_STOP_MS across src returns ONLY comments (DecisionEngineMerged.java:1012/1014/1015, BotMain.java:170). The only in-code time value is BotMain.java:178 VERIFIER_TIME_STOP_MS, which resolves PAPER tracked signals for stats, not positions. Rea |
| `DecisionEngineMerged:1228` | comment | **stale** | "Why this is safe (no whipsaw, no double-fire): Only ONE strategy fires per pair per cycle (router selects) — csLastSignalTime is GLOBAL across all strategies (cross-lock) — correlationGuard in Signal | csLastSignalTime is declared at DecisionEngineMerged.java:1520 and is never read or written anywhere (grep: only the declaration plus prose at 1218/1228/1333). generate() does not exist in the repo (grep `generate(` hits only comments at 265/1212/1216/1343/1571); the router and all strategies were cut in v88.2 (f7e663d, DecisionEngineMerged 4855->3147, `cut analyze()/candle strategies`). SignalSender.correlationGuard |
| `DecisionEngineMerged:1505` | comment | **false** | "ОТКАТ БЕЗ ДЕПЛОЯ: env CS_TP1_R=1.0 на Railway" — i.e. exit geometry is env-tunable without redeploy (same for CS_SL_ATR_MULT / CS_TP2_R / CS_COOLDOWN_MIN / CS_TIME_STOP_BARS "the hard invariant", lin | CS_TP1_R (1507), CS_TP2_R (1508), CS_SL_ATR_MULT (1497), CS_COOLDOWN_MS (1513), CS_TIME_STOP_BARS_M15 (1515), CS_SKIP_MEME (1517), CS_SIGMA_THRESHOLD/CS_VWAP_WINDOW/etc. are all read from env at class-init and then referenced NOWHERE (grep of each name in src returns only its own declaration line). SL/TP now come verbatim from the Supabase row: SupabaseSignalBridge.java:325-343 (entry/sl/tp1/tp2 read from JSON, tp1/t |
| `RiskGuard:16` | javadoc | **false** | "ПЕРЕД исполнением каждой сделки спросить у RiskGuard 'можно?' через canTrade(). Если нельзя — он скажет почему, и сделка не открывается." | canTrade() is defined at RiskGuard.java:206 and has ZERO call sites repo-wide. `grep -rn RiskGuard --include=*.java .` returns only comments plus BotMain.java:461-462, which does `getInstance()` + `statusLine()` and nothing else. The live order path is SupabaseSignalBridge.poll/drainOpens (scheduled at BotMain.java:416) and BinanceTradeExecutor — neither imports or calls RiskGuard. BotMain.java:452-459 states it outr |
| `RiskGuard:22` | javadoc | **false** | "DAILY LOSS LIMIT — при -10% дня (configurable) останавливает торги до начала следующих суток UTC." | The cap is evaluated only inside canTrade() (RiskGuard.java:227-246), which is never called (see line-16 finding). Nothing stops any trade. The 'до начала следующих суток UTC' half is doubly dead: the reset lives in rolloverIfNewDay() (:481), whose only callers are canTrade():208, recordTradeOpened():298 and recordTradeClosed():311 — all three have no external callers. |
| `RiskGuard:24` | javadoc | **false** | "WEEKLY LOSS LIMIT — при -20% за 7 дней останавливает до ручного включения через RESUME_AFTER_WEEKLY_BLOCK env." | RESUME_AFTER_WEEKLY_BLOCK does not exist anywhere in the repo — `grep -rn RESUME_AFTER_WEEKLY_BLOCK` matches only this javadoc line (RiskGuard.java:25). No envInt/envDouble/envLong/getenv call reads it (:597-608 read only RG_* keys). The real unlock is manualResume() (:379), which has no callers, and TelegramBotSender.java exposes only sendMessageAsync/shutdown/flushAndShutdown/getQueueSize/getTotalSent/getTotalDropp |
| `RiskGuard:26` | javadoc | **stale** | "DAILY TRADE LIMIT — не более N сделок в сутки (default 3)." | The actual default is 8, not 3: RiskGuard.java:90 `envInt("RG_DAILY_TRADE_LIMIT", 8)` (tagged [v86.27], previously 30 for demo). The doc number was never updated. (The limit is unenforced anyway — checked at :262-267 inside the dead canTrade().) |
| `RiskGuard:29` | javadoc | **stale** | "CONCURRENT POSITIONS — не более N одновременно открытых позиций (default 2). Защита от каскадного слива на синхронных движениях рынка." | Actual default is 1, not 2: RiskGuard.java:91 `envInt("RG_MAX_CONCURRENT_POSITIONS", 1)`. The 'защита' part is also inert: the check at :270-279 reads openPositions, which is only ever populated by recordTradeOpened() (:300) — a method with no callers, so openPositions is permanently empty. The only concurrency cap on the live path is BRIDGE_MAX_OPEN (SupabaseSignalBridge.java:71, default 20). |
| `RiskGuard:32` | javadoc | **false** | "COLD START — первые 24ч после рестарта/после weekly-block режим только Telegram-confirmation." | There is no Telegram-confirmation mode anywhere: isColdStart() (:396) is referenced only inside this file at :445 and :476 (status strings) and :452 (HealthSnapshot); no external caller exists, and TelegramBotSender has no inbound command/confirmation handling. The 'после weekly-block' half is false in the code itself: startupTime is a final field assigned once in the constructor (:97, :120) and is never re-set when  |
| `RiskGuard:61` | comment | **stale** | "openPositions + btcCrashBlock stay transient (reconciled live by PositionTracker / re-detected from BTC price)." | PositionTracker.java was DELETED in v88.2 (git log --diff-filter=D: commit f7e663d removed PositionTracker.java along with 6 other classes); `grep -rn 'class PositionTracker'` → no matches. Nothing reconciles openPositions — its only writer is the uncalled recordTradeOpened() (:300). btcCrashBlockUntil is only set inside updateBtcPrice() (:357), which also has no callers, so it is never re-detected either. |
| `RiskGuard:86` | comment | **stale** | "[v82.8] dailyTrades 8→30, maxConcurrent 2→5. ⚠️ RISK: 5×2%=10% депо разом" | The two lines directly beneath it now set 8 and 1, not 30 and 5: RiskGuard.java:90 (`RG_DAILY_TRADE_LIMIT`, 8, tagged [v86.27] 'real default (was 30 for demo)') and :91 (`RG_MAX_CONCURRENT_POSITIONS`, 1, '[v86.27] real default (was 5 demo)'). The '5×2%=10%' risk arithmetic describes a configuration that no longer exists. |
| `RiskGuard:99` | logbanner | **false** | Startup log banner: "[RiskGuard] init: dailyLoss=%.1f%% weeklyLoss=%.1f%% dailyTrades=%d maxConcurrent=%d btcCrash30m=%.1f%% btcCrash60m=%.1f%% coldStart=%dh" | The printed numbers are read correctly from the fields, but the banner announces six protections none of which are applied to any trade: all of them are only evaluated inside canTrade() (:227, :250, :263, :274, :282), which has no callers (see line-16 finding). BotMain.java:462 was explicitly amended to print 'RiskGuard (NOT ENFORCED — see project_state id=25)' and BotMain.java:494-497 removed the same limits from th |
| `RiskGuard:107` | comment | **false** | "The existing rolloverIfNewDay() on the first canTrade() will archive+reset cleanly if the persisted day is no longer today (caps reset at the real UTC boundary, not on restart)." | There is no 'first canTrade()' — canTrade (:206) is never invoked, and the other two rolloverIfNewDay callers (recordTradeOpened :298, recordTradeClosed :311) are equally uncalled. So after loadState() restores a persisted day (:568), currentDayUtc/dailyPnlUsd/dailyTradesOpened/manualLock are latched forever; nothing ever archives to weeklyPnlHistory or resets at the UTC boundary. |
| `RiskGuard:147` | javadoc | **stale** | "Live sum of OPEN unrealized PnL (USDT) across all open positions, pushed by the PositionTracker poll thread every cycle. Read by canTrade() so an open position sitting at a big loss throttles NEW ent | PositionTracker was deleted in v88.2 (f7e663d). setOpenUnrealizedPnl (:163) has no callers anywhere, so openUnrealizedPnl is permanently 0.0 and the term at :228 always contributes nothing — and the gate that would read it (canTrade) is itself unreachable. No throttling of any kind happens. |
| `RiskGuard:200` | javadoc | **false** | "Главная проверка: можно ли открывать новую позицию по symbol сейчас." (canTrade javadoc) | canTrade (:206) governs nothing — no call site exists repo-wide. Positions are opened by SupabaseSignalBridge.drainOpens → BinanceTradeExecutor without consulting it; BotMain.java:469 records this explicitly: 'The only limit that actually applies on the live path is BRIDGE_MAX_OPEN inside SupabaseSignalBridge; RiskGuard.canTrade() is NOT consulted'. |
| `RiskGuard:219` | comment | **false** | "We now test (realized + min(0, openUnrealizedPnl)) against the daily-loss cap so an open drawdown blocks new entries in real time." | Nothing blocks in real time or otherwise: openUnrealizedPnl is never written (setOpenUnrealizedPnl :163 has no callers) so the min() term at :228 is always 0, and the enclosing canTrade() (:206) is never invoked from any live path (BotMain.java:452-459, SupabaseSignalBridge.java:86). |
| `RiskGuard:256` | comment | **false** | "saveState(); // [v86.49] persist the lock so a restart can't clear it" (weekly-limit lock) | This saveState() sits inside canTrade() (:206), which is never called, so the weekly lock is never raised and never persisted. The only other lock setter, manualHalt() (:387), also has no callers. Combined with manualResume() (:379) having no callers and TelegramBotSender having no inbound command handling, the manualLock flag can be neither set nor cleared at runtime in the current build. |
| `RiskGuard:317` | comment | **false** | "saveState(); // [v86.49] persist realized daily PnL → daily-loss cap survives restart" | recordTradeClosed (:311) has no callers, so dailyPnlUsd is never updated and never persisted. All four saveState() call sites (:256 in canTrade, :305 in recordTradeOpened, :317 here, :383/:391 in manualResume/manualHalt, :507 in rolloverIfNewDay) sit on methods with no external callers — the state file is never written at runtime. Corroborated on disk: STATE_FILE defaults to ./data/riskguard.csv (:68) and the repo's  |
| `RiskGuard:322` | javadoc | **false** | "Update BTC price for crash detector. Should be called every cycle by SignalSender (it already tracks BTC price)." | SignalSender.java contains no reference to RiskGuard at all (`grep -n RiskGuard SignalSender.java` → 0 hits); its BTCUSDT usages (:1832, :2595, :2605, :2610) are candle-cache/universe code. updateBtcPrice (:325) has no callers, so btcPriceHistory stays empty, priceAtAge (:364) would always return null, and btcCrashBlockUntil (:357) is never set — protection #4 in the header can never fire. |
| `RiskGuard:378` | javadoc | **false** | "Manually unlock after weekly limit was hit." (manualResume) | manualResume() (:379) has no callers repo-wide, and TelegramBotSender.java has no inbound command handling (only sendMessageAsync/shutdown/flushAndShutdown/getQueueSize/getTotalSent/getTotalDropped are public). If a manualLock=true ever did land in ./data/riskguard.csv it would be restored at :567/:573 on every boot with no in-process way to clear it — only by editing or deleting the file. |
| `RiskGuard:386` | javadoc | **false** | "Manually halt trading. Use as emergency stop." | manualHalt() (:387) has no callers repo-wide and no Telegram/CLI entry point exists (TelegramBotSender is send-only). Even if invoked, it would only set manualLock, which is read at :211 inside the unreachable canTrade() — it could not stop the live path (SupabaseSignalBridge.drainOpens → BinanceTradeExecutor). |
| `RiskGuard:525` | comment | **false** | "positions are reconciled live by PositionTracker on startup; the BTC-crash block re-detects from the live price feed within a minute" | PositionTracker.java was deleted in v88.2 (git commit f7e663d, --diff-filter=D); nothing reconciles openPositions on startup — the map's only writer, recordTradeOpened (:300), is uncalled. The BTC-crash block cannot re-detect either: updateBtcPrice (:325), the sole writer of btcCrashBlockUntil (:357), has no callers and SignalSender never references RiskGuard. |
| `SignalSender:121` | comment | **false** | "Real institutional-listing noise is still blocked by isBlocklisted + soft-block + SL-gate + ATR-gate chain. Keeping vol thresholds high was double-filtering." | None of the named chain runs: checkLiquidity() (:1787, the only consumer of MIN_VOL_*) has no callers; getMaxSlPct() (:970) has no callers; hotSoftBlocklist (:239) is never written; isBlocklisted() is referenced only from getTopPairsForForecast (:3627) and getScanUniverseSnapshot (:3675), both of which have no callers. |
| `SignalSender:586` | comment | **false** | "Removal: ~170 lines, no behavior change. Bot remains a pure signal scanner." (+ "Even with ENABLE_AUTO_TRADE=1 the bot would not trade") | The bot trades. BotMain.java:414-419 schedules SupabaseSignalBridge.getInstance()::poll every 30s, which drains public.bot_orders and drives BinanceTradeExecutor (SupabaseSignalBridge.java:19-22, sizing from live balance at SupabaseSignalBridge.java:271). BotMain.java:489-506 prints "Auto-trade АКТИВИРОВАН … 🔴 REAL/LIVE". The claim only ever covered the deleted executeOrderAsync, but it asserts a whole-bot property t |
| `SignalSender:767` | comment | **stale** | "Authoritative quality control further downstream: 1) Dispatcher.dispatch cold-start gate (BotMain Phase 1: floor 48) 2) ProbabilityCalibrator PAV regression (after n>=50 outcomes)" | Neither class exists — `grep -rn "class Dispatcher\|class ProbabilityCalibrator"` over the source tree returns nothing (only 8 classes remain after v88.2). MIN_CONF itself (:764) is assigned and then never read (grep MIN_CONF in SignalSender.java → :44, :764 only). |
| `SignalSender:966` | comment | **false** | "Max SL% gate: signals with stop-loss > this % of entry price are blocked. … For small accounts (<$50): max 3% SL. For normal accounts: max 5% SL." | getMaxSlPct() (SignalSender.java:970) has ZERO callers anywhere in the repo (grep -rn getMaxSlPct → only the definition). No SL-percentage gate is applied to anything. |
| `SignalSender:1446` | comment | **stale** | "PATCH #3: LEVERAGE + MARGIN MODE INITIALIZATION — Sets ISOLATED margin + 5x leverage on all active trading pairs. Called once at startup after account is ready." | initLeverageAndMarginMode (:1453) iterates wsMap.keySet() (:1455), but wsMap is never populated — the only writer is connectWsInternal (:2240), reachable only from startWebSocketsForTopPairs (:1047) ← forceResubscribeTopPairs (:1115), which has no callers. It therefore always takes the getTopSymbolsSet fallback (:1461), and is scheduled only when BINANCE_USE_TESTNET=1 (:859). "all active trading pairs" is never what  |
| `SignalSender:1980` | javadoc | **false** | "Used to surface 'saturated' state in logs without blocking — the allow() gate already does the actual blocking." | CorrelationGuard.allow() (SignalSender.java:1935) and register() (:1957) are never invoked — the only uses of the correlationGuard field are slotsSnapshot() at :3644 and isSaturated() at :3647, and those two public accessors themselves have no callers. Nothing is blocked by correlation any more. |
| `SignalSender:1992` | comment | **stale** | "Removing the reset preserves cross-cycle correlation protection." / "[v50 FIX BUG-21] State is now PERSISTENT between cycles" (max 5 same-direction, 6 total, 2 per sector) | The caps MAX_DIR_NORMAL/MAX_TOTAL/MAX_SECTOR (SignalSender.java:1883-1888) are only read inside allow()/count(), and allow() is called from nowhere (see line 1935). activeEntries is never populated, so slotsSnapshot() would always report "0L/0S 0/6". |
| `SignalSender:2139` | comment | **false** | "Сейчас авто-торговля выключена, но правка превентивная на будущее." | Auto-trade is on the live path: BotMain.java:414-419 (bridge poll) → BinanceTradeExecutor; BotMain.java:478-506 explicitly states execution goes through SupabaseSignalBridge and can be REAL/LIVE. |
| `SignalSender:2710` | telegram | **false** | Telegram boot status: "🛰 Ловец листингов активен … Новый листинг → 🆕 алерт + запись · _бот не торгует_" | Operator-facing statement that the bot does not trade, sent once per boot. The bot does trade: BotMain.java:414-419 arms SupabaseSignalBridge which places orders through BinanceTradeExecutor. BotMain.java:479-481 already killed an identical misleading banner for this exact reason in v88.2.2, but this one survived. |
| `SupabaseSignalBridge:33` | javadoc | **false** | "Real money is NOT reachable via env flags for opening new exposure" (TESTNET-LOCKED IN CODE block) | executionAllowed() (SupabaseSignalBridge.java:167-182) gates on TESTNET_SET + executor.isTestnet(). BinanceTradeExecutor.isTestnet() (BinanceTradeExecutor.java:463) returns only the raw flag set at :190 ("1".equals(BINANCE_USE_TESTNET)) — it is NOT the endpoint. The endpoint is baseUrl, and in the testnet branch baseUrl = getenv("TESTNET_BASE_URL", "https://demo-fapi.binance.com") (BinanceTradeExecutor.java:203). So  |
| `SupabaseSignalBridge:45` | javadoc | **false** | "Both skip on a failed read and both PATCH conditionally on the expected current status, so neither can clobber a concurrent transition." | True for reconcileOpen(), which PATCHes "/rest/v1/bot_orders?id=eq."+id+"&status=eq.open" (SupabaseSignalBridge.java:238). FALSE for reconcileSent(): it calls patch(id, "open"\|"failed", ...) at :206-207, and patch() builds "/rest/v1/bot_orders?id=eq."+id with NO status predicate (:413-417). A close_requested written by the live_time_closer cron between reconcileSent's GET (:198) and its PATCH is overwritten back to  |
| `SupabaseSignalBridge:100` | javadoc | **false** | "SINGLE SOURCE OF TRUTH for which endpoint we are on: taken from the executor that actually builds the URLs, never re-read from env here. Two independent reads of BINANCE_USE_TESTNET are precisely how | useTestnet = executor.isTestnet() (SupabaseSignalBridge.java:104) does remove the duplicate env read, but isTestnet() (BinanceTradeExecutor.java:463) exposes the FLAG, not the URL builder's baseUrl (:203/:207). The precise failure the comment claims to have eliminated — logging TESTNET(demo) while the executor talks to fapi.binance.com — is still reachable with BINANCE_USE_TESTNET=1 + TESTNET_BASE_URL=https://fapi.bi |
| `SupabaseSignalBridge:141` | logbanner | **false** | Boot banner reports the live endpoint: "[Bridge] polling endpoint=TESTNET(demo)\|*** REAL ***" | The banner prints from useTestnet (SupabaseSignalBridge.java:104 <- BinanceTradeExecutor.isTestnet():463 <- the BINANCE_USE_TESTNET flag at :190). It never reads baseUrl (BinanceTradeExecutor.java:203/207), so with TESTNET_BASE_URL pointed at a real host the operator-facing banner says TESTNET(demo) while orders go to the real exchange. The rest of the banner is accurate: opens=/closes=ALWAYS match poll() (136-156),  |
| `SupabaseSignalBridge:164` | javadoc | **false** | "There is no combination of environment variables that reaches real capital from here; lifting the lock is a code change" | Same path as line 33: executionAllowed() only consults BINANCE_USE_TESTNET (SupabaseSignalBridge.java:66-67, 171-180) and the derived flag executor.isTestnet(). TESTNET_BASE_URL (BinanceTradeExecutor.java:203) is an env var that repoints the so-called testnet base URL at any host including https://fapi.binance.com, while every check in this class still reports testnet. The gate's own three checks (167-182) never look |
| `TradingCore:77` | comment | **false** | CoinType declares per-class leverage caps: TOP maxLeverage=20, ALT=10, MEME(1.5, 0.0060, 5) — i.e. meme coins are capped at 5x — plus riskMultiplier and expectedSlippage per class | Repo-wide grep: `maxLeverage`, `riskMultiplier`, `expectedSlippage` and `CoinType.` appear ONLY at TradingCore.java:79-86 — zero readers in all 8 java files. The only leverage actually applied is a single global value clamped to 1..10 in BinanceTradeExecutor.java:212 (`this.leverage = Math.max(1, Math.min(10, lev))`), applied per-symbol at BinanceTradeExecutor.java:687 `initSymbolLeverage(symbol)` with no coin-class  |
| `TradingCore:607` | javadoc | **false** | atrPercentile is "Used to calibrate position sizing and signal confidence" | `grep -rn atrPercentile` over the whole repo returns exactly one hit — the definition at TradingCore.java:627. No caller exists in BotMain/SignalSender/DecisionEngineMerged/SupabaseSignalBridge/BinanceTradeExecutor/RiskGuard. Position sizing is computed in BinanceTradeExecutor (balance x risk x leverage, see BinanceTradeExecutor.java:623) and never consults volatility percentile. |

## Ложные и устаревшие — уровень поведения (96)

| Файл:строка | Вид | Вердикт | Утверждение | Проверка |
|---|---|---|---|---|
| `BinanceTradeExecutor:67` | javadoc | **stale** | «SLIPPAGE PROTECTION … Если spread > 0.5% — отказываемся» (repeated in the env table: MAX_SPREAD_PCT = 0.5) | the default is now 0.30% on real and 1.0% on testnet (:232), env-overridable; the actual check at :701-704 compares against maxSpreadPct. Real is stricter than documented, testnet 2x looser. The env table at :79 repeats the same stale 0.5. |
| `BinanceTradeExecutor:123` | comment | **false** | «symbols where we have already set leverage + isolated» (initializedSymbols) | :677 adds the symbol to initializedSymbols on the testnet path without issuing either POST, so membership does not mean leverage+isolated were set. |
| `BinanceTradeExecutor:623` | javadoc | **false** | «3. Calculate position size from balance × risk × leverage.» | :775-776 qty = (balanceUsd × riskPct/100) / \|entry − stop\|; leverage never enters the quantity. It appears only in marginUsed (:844) and the liquidation-distance guard (:756). |
| `BinanceTradeExecutor:815` | comment | **stale** | «Теперь нижний потолок капа = minNotional + 2 шага qty (в $), что ГАРАНТИРУЕТ: после округления qty вниз нотионал останется > minNotional.» | the minTradable floor (:821-824) is applied first, then the later v86.27 absolute cap EXEC_MAX_NOTIONAL_USD (default $6.0, :838-842) re-cuts notional back below it; :853 rounds down and :858 then rejects with 'notional < min' for every symbol whose minNotional > $6 — the consequence is acknowledged at :834-837, which voids the earlier guarantee. |
| `BinanceTradeExecutor:1399` | javadoc | **false** | «Fetch current position size on the exchange. >0 = long, <0 = short, 0 = none.» | 0 is also returned on a non-200 response (:1414) and on any exception (:1425) — precisely the ambiguity that forced fetchPositionAmountChecked (:1437) into existence. Current in-file callers (:1042, :1138, :1204) all guard with >0 so no live mis-read today, but the documented contract is wrong and invites the phantom-flat bug class. |
| `BotMain:35` | javadoc | **stale** | [I6] "Public verification API: forecastIntegrityCheck() — даёт способ проверить, что бот не 'потерял' выпавшие исходы" / "Public API for third-party verification. Returns JSON-ish string." | No method named forecastIntegrityCheck exists anywhere (grep over com/bot returns only this javadoc line). The dangling javadoc at BotMain.java:568 documents a method that was deleted; lastIntegrityReportMs (line 566) has no readers. |
| `BotMain:38` | javadoc | **stale** | [I7] "Forecast records persistence: при рестарте старые unresolved сигналы НЕ теряются" / "survive Railway restarts" | FORECAST_PERSIST_FILE (BotMain.java:219-220) has no readers and no load/save method exists; forecastRecords (line 215) is never put to or read (only declared), and the section header at line 556-558 sits over empty space. ForecastRecord.escape() is dead private code. Nothing is persisted or restored. |
| `BotMain:52` | javadoc | **stale** | audit map: "'ProbabilityCalibrator не в коде' — ЛОЖЬ, класс ЕСТЬ в DecisionEngineMerged" | True when written, false now: DecisionEngineMerged.java:221 reads "[project_state id=14, id=23] ProbabilityCalibrator removed. Its write path was dead". No such class remains; only ./data/calibrator.csv (102 bytes) and stale comments (SignalSender.java:769) reference it. |
| `BotMain:113` | comment | **stale** | LEGACY_BT: "Live candle scan stays (paper, mostly silent) — only the backtests are gated. Re-enable everything with LEGACY_BT=1. NOT a delete — fully reversible." | LEGACY_BT (BotMain.java:115) is read by no code at all. The live candle scan it says "stays" was deleted in v88.2 — DecisionEngineMerged.analyze() has zero callers repo-wide — so LEGACY_BT=1 restores nothing and the change is not reversible by env. |
| `BotMain:129` | comment | **false** | NEW_LISTING_CATCHER: "Default ON; disable with NEW_LISTING_CATCHER=0." | The constant (BotMain.java:131) is read by no code — SignalSender only mentions it in comments (SignalSender.java:387, 2623). BotMain schedules sender.checkNewListings() unconditionally inside the DataCatchers task (BotMain.java:425-431), and checkNewListings (SignalSender.java:2632) contains no flag check. Setting NEW_LISTING_CATCHER=0 changes nothing. |
| `BotMain:136` | comment | **false** | FUNDING_SNAPSHOT: "Each cycle snapshots symbols with extreme funding to ./data + Supabase… Default ON; disable with FUNDING_SNAPSHOT=0." | Flag read nowhere (only SignalSender.java:2998 comment); snapshotFundingExtremes() is called unconditionally from BotMain.java:428 and has no gate. Worse, it iterates SignalSender.cachedPairs (SignalSender.java:3010), which is assigned only in forceResubscribeTopPairs() (line 1115-1120) — a method with zero callers — so the set is permanently empty and no snapshot is ever written. |
| `BotMain:141` | comment | **false** | LIQ_CAPTURE: "Buffers liquidation events from the existing WS stream and flushes a batch per cycle to ./data + Supabase." | Buffering works (SignalSender.java:3525 checks BotMain.LIQ_CAPTURE), but the drain half does not exist on any path: flushLiquidations() (SignalSender.java:3249) has no callers — BotMain's DataCatchers task calls only checkNewListings/snapshotFundingExtremes/trackBreakoutSignals (BotMain.java:426-430). Events accumulate to LQ_BUFFER_MAX and are then dropped; nothing reaches ./data or Supabase. |
| `BotMain:149` | comment | **false** | TREND_TRACK: "Logs fresh daily breakout signals for forward-testing… Default ON… Disable with TREND_TRACK=0." | Flag read nowhere (only SignalSender.java:3076 comment); trackBreakoutSignals() is called unconditionally (BotMain.java:429) and its first real statement returns when cachedPairs is empty (SignalSender.java:3082-3083) — which it always is, since forceResubscribeTopPairs() (the sole assignment, SignalSender.java:1115-1120) is never called. No breakout row is ever logged. |
| `BotMain:154` | comment | **stale** | startup banner reports "X-EXCHANGE_CHECK=ON/OFF", and the field comment promises "If discrepancy >0.5% → log warning + dispatch blocked" | CROSS_EXCHANGE_VALIDATION (BotMain.java:156) is referenced only by the banner at BotMain.java:450. The helper it advertises is gone — all that survives is its header comment (BotMain.java:333-343) and the orphan map xCheckLastErrorMs (line 344). No Bybit/OKX call exists in the tree, so the banner can print ON for a check that cannot run. |
| `BotMain:159` | comment | **false** | KLINES: "default scales with PRIMARY_TF … 1h primary: 168 bars = 7 days" | BotMain.KLINES (line 162-163) is read by nothing. The value actually used for fetching is SignalSender.KLINES_LIMIT = envInt("KLINES", 420) (SignalSender.java:770), a flat 420 that does not scale with PRIMARY_TF, and it ignores the KLINES_LIMIT alias BotMain documents. |
| `BotMain:168` | comment | **stale** | "Time-stop window for the verifier — single source of truth across bot" (VERIFIER_TIME_STOP_MS / GRACE / MAX_AGE), incl. [I4] "maxAgeMs синхронизирован с ISC TIME_STOP_BARS" | There is no verifier left: VERIFIER_MAX_AGE_MS (BotMain.java:183) has no readers, checkForecastAccuracy is gone (only its header comment at line 544 remains), and the ISC class it claims to mirror does not exist in the tree. The constants are computed at class-init and never used. |
| `BotMain:273` | comment | **false** | regimeOutcomes: "key → int[2]{total, wins}; персистится в forecast-файле (#REGIME\| строки)" | regimeOutcomes (BotMain.java:273) is never written, read or serialized — no "#REGIME" literal exists anywhere in the tree, and the forecast file it names is itself never written (FORECAST_PERSIST_FILE unused). Same for forecastTotal/forecastCorrect (line 265-266) and signalOutcomes (line 290). |
| `BotMain:274` | comment | **stale** | [I1]/[I2] counters "visible in stats so user can see 'out of 100 signals: 60 wins, 30 losses, 10 ambiguous half-credit'" / "so user can distinguish real SL from didn't move" | forecastAmbiguous (line 276) and forecastTimeStop (line 279) are never incremented and never rendered — the stats/Telegram reporting path that displayed them was deleted along with checkForecastAccuracy; no Telegram text in BotMain mentions them. |
| `BotMain:421` | comment | **false** | "[data-catchers] Keep the Supabase data feeds alive: nl_micro (new listings), funding_snaps (funding extremes), trend_signals (breakouts)… the data pipeline stays." | Only nl_micro survives: checkNewListings fetches exchangeInfo itself (SignalSender.java:2637-2643). funding_snaps and trend_signals both enumerate cachedPairs (SignalSender.java:3010, 3082), and cachedPairs is written only by forceResubscribeTopPairs (line 1120), which lost its caller when the scan cycle was cut in v88.2 — repo-wide grep finds no invocation. Two of the three feeds emit nothing. |
| `BotMain:448` | logbanner | **false** | startup banner: "TradingBot v88.2.2 started … (first cycle in 90s, …)" | Nothing is scheduled at 90s and no "cycle" exists after v88.2: mainSched runs SupabaseBridge at 120s/30s (BotMain.java:415-417), auxSched runs DataCatchers at 60s + INTERVAL (line 425-431) and TimeSync at 5min (line 433-435). The candle scan cycle the phrase referred to was deleted. |
| `BotMain:527` | comment | **stale** | "[v87.6] NEW-strategy backtest summary (breakout)… Disable with BREAKOUT_BT=0." | The task is submitted (BotMain.java:531), but runBreakoutBacktest returns on its second line — `pairs = new ArrayList<>(cachedPairs); if (pairs.isEmpty()) return;` (SignalSender.java:3158-3159) — and cachedPairs is never populated (sole assignment SignalSender.java:1120 in the uncalled forceResubscribeTopPairs). The honest per-year/survivorship summary at SignalSender.java:3190-3197 is unreachable; no Telegram messag |
| `BotMain:544` | comment | **stale** | "checkForecastAccuracy — REWRITTEN with full outcome accounting… TP1 hit → calibrator hit=true… В audit log пишется КАЖДЫЙ outcome с full payload + HMAC." | Comment block with no method under it — checkForecastAccuracy does not exist (grep finds only this line), the calibrator it feeds was removed (DecisionEngineMerged.java:221) and no audit-log/HMAC writer remains in BotMain. |
| `BotMain:561` | comment | **stale** | [I6] "Daily integrity report — public-verifiable stats sent to Telegram… показывает обе цифры рядом — если они расходятся, видно сразу." | No such report exists: the only surviving artifact is lastIntegrityReportMs (BotMain.java:566), which is never read or written, and no scheduled task in main() sends stats to Telegram. |
| `BotMain:574` | comment | **stale** | "HEARTBEAT 90 min → 6h… Теперь только каждые 6h, и только если 6h без signals. Реальная информация о трейдах приходит через notifyTradeOutcome() при resolution каждого signal." | HEARTBEAT_INTERVAL_MS / HEARTBEAT_QUIET_MS (BotMain.java:578-579) and lastHeartbeatMs (573) have no readers; nothing is scheduled to emit a heartbeat. notifyTradeOutcome does not exist anywhere in the tree, so no trade-outcome message is sent either. |
| `BotMain:585` | comment | **stale** | "[STARTUP-BACKTEST] Full one-shot backtest at boot. Pre-trains the ProbabilityCalibrator on historical data across the entire trading universe so that LIVE signals start with calibrated probabilities, | Header over an empty section: no startup candle backtest is submitted in main() (the only heavySched job is runBreakoutBacktest, BotMain.java:531) and the calibrator it claims to pre-train no longer exists (DecisionEngineMerged.java:221). |
| `DecisionEngineMerged:163` | comment | **false** | "[HOLE-5 FIX] MIN_CONF_FLOOR 48 → 52 ... Поднимаем floor до 52, чтобы ранний reject экономил CPU. Authoritative env-MIN_CONF остаётся в SignalSender." | MIN_CONF_FLOOR (165) is never read in the repo (grep: declaration + prose at 133/161 only; MIN_CONF_CEIL is used at 384, the floor is not). DE performs no reject and emits no ideas at all after v88.2, so there is no early reject to save CPU. SignalSender.java:764-765 separately documents "MIN_CONF=50 == DE.MIN_CONF_FLOOR" against a DE value of 52 — the two are not in sync either. |
| `DecisionEngineMerged:184` | comment | **false** | "Метод clustersRequired(MarketState) ниже возвращает нужное значение в runtime" — adaptive 2/3 agreeing-cluster requirement | No method clustersRequired exists anywhere in src (grep `clustersRequired` = this comment only). MIN_AGREEING_CLUSTERS (186), MIN_AGREEING_CLUSTERS_RANGE (187) and MIN_CLUSTER_SCORE (192) are never read by any code in the repo. No cluster gate runs at runtime. |
| `DecisionEngineMerged:227` | comment | **false** | "[v67] Reject trace — per-reason counter, deltas printed each cycle via getAndResetRejectTrace(). Lets SignalSender's [DIAG] line show WHICH internal gate is killing 24 of 25 pairs." | No method getAndResetRejectTrace exists anywhere in src (grep returns only this comment). reject(String) at DecisionEngineMerged.java:232 has zero call sites (grep `reject(` in the file: declaration at 232 plus a prose mention at 1343). REJECT_TRACE (230) is therefore always empty and no [DIAG] line can report anything. |
| `DecisionEngineMerged:238` | comment | **false** | "[v42.0 FIX #12] Last GC timestamp for postExitCooldown leak fix" (with POST_EXIT_MAX_SIZE cap) | lastCooldownGcMs (239) and POST_EXIT_MAX_SIZE (240) are referenced nowhere else in src (grep returns only the declarations). No GC/eviction code for postExitCooldown exists; the map is put-only at 387 and 408 and grows unbounded for the process lifetime. |
| `DecisionEngineMerged:247` | comment | **false** | "CVD must persist for 2 bars before it counts as confirmation... CVD must remain a real confirmation filter." | CVD_PERSIST_BARS (251) is never read (grep in src: declaration only). cvdHistory is written in setCVD (418-421) and never read; cvdMap is written at 416 and never read. Live producer SignalSender.java:2373 calls setCVD every cycle into a write-only store. There is no CVD filter, persistent or otherwise. |
| `DecisionEngineMerged:264` | javadoc | **stale** | Dangling javadoc: "Optional ISC reference — used only for chain-pause check at the top of generate(). Null until BotMain wires it; null-safe at every call site." | The javadoc documents a field that no longer exists — line 267 is the constructor `public DecisionEngineMerged() {}`. No ISC/InstitutionalSignalCore class exists (deleted in v88.2, git f7e663d), and generate() does not exist in the repo. Nothing pauses anything. |
| `DecisionEngineMerged:391` | javadoc | **stale** | "Record win — decays confidence penalty, resets consecutive losses. Call from BotMain TradeResolver after TP hit." | Two parts stale. (1) The consumer is gone: symbolMinConf is written only via compute() at 382 and 397 and is never read anywhere in the repo, so the loss penalty / win decay influences no decision (globalMinConf likewise only read inside those two computes). (2) The stated caller is wrong: the only caller is SignalSender.processUserDataEvent (SignalSender.java:1592/1594), triggered by any UDS close with realizedPnl>0 |
| `DecisionEngineMerged:466` | javadoc | **false** | "Robust ATR: max(currentATR, longTermATR × 0.80)." (method javadoc formula) | The body returns max(longTermAtr*0.80, 0.70*longTermAtr + 0.30*fastAtr) at line 506-509 — a 70/30 blend, not max(current, 0.8*longTerm); and for series shorter than fastN+50 it returns bare fastAtr with no floor (line 497), while for series shorter than fastN+2 it returns TradingCore.atr over a shrunken period (491). The [v43] comment at 475-489 describes the real behaviour; the javadoc header above it was never upda |
| `DecisionEngineMerged:862` | telegram | **stale** | The trader-facing signal card itself (toTelegramString: Grade, Кластеров, Скор %, Контекст flags, Time-stop, tight-SL and thin-liquidity warnings) | toTelegramString() has no caller in the repo; its only entry point is toString() (1025), and no code stringifies a TradeIdea (grep for `+ idea`/`idea +`/String.valueOf(idea) finds only SupabaseSignalBridge.java:348/350 which concatenate idea.tp1/idea.tp2 doubles). The live path builds an idea and hands it straight to executor.openPositionWithSl (SupabaseSignalBridge.java:267-274). Additionally, if it did render for a |
| `DecisionEngineMerged:931` | comment | **false** | "Dispatcher filters sub-30-sample signals upstream, so any signal reaching Telegram has earned its score through strict gates (prob≥78, clusters≥4)." | There is no Dispatcher class in src (grep `Dispatcher` = comments only; BotMain 3498->806 in v88.2 explicitly `cut Dispatcher/runCycle/backtest`, git f7e663d). The only live TradeIdea producer is SupabaseSignalBridge.buildLongIdea (SupabaseSignalBridge.java:336-339) which hardcodes probability=0.95 on the 0..100-scaled field, passes no clusters, and passes flags {SUPABASE, kind}. No gate of any kind stands between th |
| `DecisionEngineMerged:933` | comment | **stale** | "NOTE: probability is already clamped to PROB_CEIL=85 at line ~3353 (calibrate), so no extra clamp needed here." | The file is 1771 lines; there is no calibrate() method and no clamp. PROB_CEIL (declared line 198) is referenced by zero code — grep PROB_CEIL in src returns line 198 and this comment at 933 only. Its own javadoc at 196-197 ("All intermediate caps and the final calibrator clamp must reference this constant") is equally unbacked. |
| `DecisionEngineMerged:953` | comment | **false** | "The strategy already stores the authoritative count via setAgreeingClusters (TREND=3, VCB=5/4, funding=1) — the SAME value the BotMain quality gate enforces (getAgreeingClusters). Use it; fall back t | Repo-wide grep for getAgreeingClusters/setAgreeingClusters: DecisionEngineMerged.java:664/665/955/956 and SignalSender.java:3398/3400 (inside rebuildIdea, which has no callers). BotMain contains no reference at all — there is no BotMain quality gate enforcing it. No producer ever calls setAgreeingClusters (the strategies that did were deleted in v88.2), so agreeingClusters stays -1 and line 955-956 always falls back  |
| `DecisionEngineMerged:1065` | javadoc | **stale** | "It is NOT a calibrated win-rate (calibration activates after ~50 resolved trades)" / "Calibration activates after 50+ resolved trades" | There is no calibrator left to activate: lines 221-225 of this same file record that ProbabilityCalibrator was removed because recordOutcome*() was never called; no such class exists in src (grep ProbabilityCalibrator = comments only, in BotMain.java:18/52/585 and here). Calibration never activates at any sample count. Same stale promise repeats at line 1072-1073. |
| `DecisionEngineMerged:1362` | comment | **false** | "Set PHASE_MR_ENABLE=false to run pure trend-pullback (Breakout-only) experiment. Default true = legacy behavior." (also PHASE5_FUNDING_MOMENTUM_ENABLE / PHASE5_* tunables) | PHASE_MR_ENABLE (1364), PHASE5_FUNDING_MOMENTUM_ENABLE (1365), PHASE5_FUNDING_THRESHOLD (1366), PHASE5_SL_ATR_MULT (1367), PHASE5_TP_R (1368) are each referenced only on their own declaration line (repo-wide grep). The router and the MR/Breakout/FundingMomentum strategies were deleted in v88.2, so no env value changes any behaviour. |
| `DecisionEngineMerged:1471` | comment | **false** | "To revert to 15m defaults set env CS_PROFILE=15m (overrides below)." | CS_PROFILE is read nowhere (grep in src = this comment only). The actual switch is CS_PRIMARY_TF = getenv("PRIMARY_TF") at line 1479, feeding CS_IS_15M/CS_IS_4H/CS_IS_30M (1480-1482). Setting CS_PROFILE has no effect (and the constants it would steer are themselves unused). |
| `DecisionEngineMerged:1566` | javadoc | **stale** | "Now lastSigPrice is updated ONLY in confirmSignal() after ISC approves" + the dynamic price-moved gate max(0.20%, ATR×0.15) | Literally true inside the file (the only lastSigPrice.put is at 366) but the whole mechanism is unreachable: confirmSignal (364/370) has no caller outside DE (repo grep), so lastSigPrice is never populated; priceMovedEnough (1572/1583) has no caller either — its only reference is its own overload at 1584. There is no ISC to approve anything (class deleted in v88.2). The de-duplication gate does not run. |
| `DecisionEngineMerged:1642` | javadoc | **false** | "Wilder's Smoothed ATR — matches TradingView/Binance exactly." | This atr() seeds the RMA only over the last 2*period bars (seedStart = c.size() - period*2, line 1651) instead of the whole series, so ~36% of the final value still carries the truncated SMA seed. TradingCore.atr (TradingCore.java:159-172) — the one actually used by robustAtr at lines 491/494/501 — seeds at index 1 over full history, so the two disagree on identical data. The file's own rsi javadoc at 1743-1745 cites |
| `RiskGuard:36` | javadoc | **stale** | "Состояние держится в памяти, сбрасывается при рестарте Railway. Это намеренно: после рестарта daily counters обнуляются" | Contradicted by the [v86.49] persistence added later in the same file: STATE_FILE ./data/riskguard.csv (:67-68), saveState() (:527) and loadState() (:547) called from the constructor at :110, restoring currentDayUtc/dailyPnlUsd/dailyTradesOpened/manualLock (:568-574). The header text was left describing the pre-v86.49 design, and the :106-109 comment says the opposite ('a restart does NOT clear the loss caps'). |
| `RiskGuard:152` | javadoc | **stale** | "it's reconciled live by the tracker within one poll (~30s) after a restart, so persisting it would only risk latching a stale value" | The 'tracker' is PositionTracker, deleted in v88.2 (f7e663d); no polling thread writes openUnrealizedPnl — setOpenUnrealizedPnl (:163) has no callers. The remaining 30s poll comment in the codebase (SignalSender.java:842) also refers to the deleted PositionTracker. |
| `RiskGuard:226` | comment | **stale** | "rate-limited to 2 polls on the PositionTracker side — acceptable, fail-safe" | There is no PositionTracker side any more — PositionTracker.java was deleted in v88.2 (f7e663d) and `grep -rn 'class PositionTracker'` finds nothing, so no such rate-limiting code exists to back the claim. |
| `RiskGuard:395` | javadoc | **false** | "True if the bot is in cold-start period (first 24h). Used by caller to optionally enforce Telegram-confirmation mode." | isColdStart() (:396) has no external callers; its only uses are internal cosmetics — :445 (health state string), :452 (HealthSnapshot field), :476 (statusLine ' COLD' suffix). No Telegram-confirmation mode exists in the codebase; TelegramBotSender cannot receive replies. |
| `SignalSender:205` | comment | **stale** | "Solution: Every aggTrade tick, measure 30-second price delta. If delta > threshold — immediately submit that pair for a full processPair() run outside the normal cycle. This reduces reaction time fro | HOT_PAIR_TOP_PCT/ALT/MEME (:229-231), HOT_PAIR_COOLDOWN_MS (:236) and HOT_PAIR_MAX_CONCURRENT (:244) are declared and never referenced anywhere in the repo; processAggTrade (:2319) never checks them. |
| `SignalSender:257` | comment | **stale** | "earlyTickFlusher drains the buffer, sorts by probability, and dispatches only the TOP-1 per pair. Prevents signal spam…" / EARLY_TICK hourly cap of 4 per pair | No earlyTickFlusher exists. The rate-limit helpers earlyTickHourlyLimitReached (:286) and recordEarlyTickSent (:294) have no callers, and ENABLE_EARLY_TICK (:783) is assigned and never read. |
| `SignalSender:432` | comment | **false** | "[v87.0] LIQUIDATION CAPTURE … we buffer each event there and flush a batch once per cycle → ./data + Supabase." | Same as above — flushLiquidations() (:3249) has no caller; the batch write at :3269-3272 never executes. |
| `SignalSender:726` | comment | **false** | "Fix: bounded queue(100) + DiscardPolicy → drops oldest pending HTTP task, never blocks the scheduler, never crashes the JVM." | The pool is built with ThreadPoolExecutor.DiscardPolicy() (:736), which silently discards the NEWLY SUBMITTED task, not the oldest queued one — dropping the oldest is DiscardOldestPolicy (used deliberately for fetchPool at :827 with the opposite comment at :810-812). The "never blocks" half is correct; the "drops oldest" half is inverted. |
| `SignalSender:771` | comment | **stale** | "[v66] 160 → 420. CRITICAL BUG FIX: processPair gate at line 1149 requires m15.size() >= 400 … The main cycle path has been dead since v50" | There is no processPair method and no 400-bar gate; line 1149 is now inside recordStaleEvent. KLINES_LIMIT (:770) is still read, but only by getPrimaryTfCandles (:1192), getCached15mWithLive (:1196) and getM1FromWs (:1354) — none of which have live callers. |
| `SignalSender:841` | comment | **stale** | "Core trading does NOT need it: PositionTracker polls positionRisk via REST every 30s for close/PnL/management." | No PositionTracker class exists in the source tree (grep "class PositionTracker" → nothing; only 8 classes remain in com.bot). Position management now lives in BinanceTradeExecutor/SupabaseSignalBridge. |
| `SignalSender:882` | logbanner | **false** | Startup banner "[SignalSender v7.0] TOP_N=%d SCAN=%d … LIVE_CANDLE=ON WS_AUTO=ON …" | WS_AUTO=ON and LIVE_CANDLE=ON are hardcoded literals reporting subsystems that can no longer run: startWebSocketsForTopPairs (:1047) has no reachable caller, so no per-pair WebSocket is ever opened (wsMap stays empty); the live-candle path getPrimaryTfCandles (:1188)/getCached15mWithLive (:1195) has no callers. TOP_N/SCAN are printed but the scan itself (selectPairsForScan :1021, computePairBudget :1007) is dead too. |
| `SignalSender:909` | comment | **stale** | "This allows InstitutionalSignalCore.autoBlacklist to push symbols here at runtime when their win-rate drops below 25% after 5+ trades." | No InstitutionalSignalCore class exists. addToGarbageBlocklist (:956), the only mutator of GARBAGE_COIN_BLOCKLIST, has no callers — the set is effectively the static seed only. |
| `SignalSender:955` | javadoc | **stale** | "[v43] Called by BotMain after ISC auto-blacklist fires — syncs to processPair filter" | grep -rn addToGarbageBlocklist → definition only (:956). BotMain never calls it, and processPair does not exist. |
| `SignalSender:1143` | javadoc | **stale** | "[v78.1] Record a stale event; trigger 30-min skip if 3 stales in 5min window." | Constants say otherwise: STALE_TRIGGER_COUNT = 8 (:630) and STALE_SKIP_DURATION_MS = 15 min (:631) after the 2026-05-05 loosening. The method is also dead — recordStaleEvent (:1145) and isChronicallyStale (:1133) have no callers. |
| `SignalSender:1168` | javadoc | **stale** | "Splice is gated: the live bar is included only when it is in a neutral observation window. If the live bar is already mid-impulse (body > 0.60×ATR), at an RSI extreme (outside 28..72), or showing wic | isLiveCandleSafeToSplice (:1245-1251) unconditionally returns true — the only check left is `if (closedHistory.size() < 15) return true;`. The v77 rewrite removed every veto (documented at :1236-1243) but this older javadoc block was left in place above it. |
| `SignalSender:1222` | javadoc | **false** | "Returns false if splicing the live candle into the analysis series would bias the decision engine toward the local top/bottom of the in-flight bar." | The method (:1245) has no return-false path at all; both branches return true (:1248, :1250). |
| `SignalSender:2402` | javadoc | **stale** | "EARLY_TICK buffer flush — drains earlyTickBuffer atomically, sorts by age-decayed probability, dispatches up to MAX_EARLY_PER_FLUSH per cycle." | Orphan javadoc attached to a bare constant; no flush method exists. earlyTickBuffer (:261) is never read or written anywhere (grep), and the related EARLY_STALE_DROP_MS/EARLY_STALE_DECAY_MS (:2416-2417) are unused. |
| `SignalSender:2419` | comment | **stale** | "HOT PAIR RESCAN — triggered from aggTrade handler when 30-tick price delta exceeds threshold. Runs a full processPair() in the background fetchPool. Result: if valid signal found → sent to Telegram i | processAggTrade (:2319-2395) contains no hot-pair logic. hotPairTotalTriggers is only read by the 10-minute monitor at :876 and never incremented; hotPairLastRescan/hotPairActiveCount (:245-246) are never touched. The whole rescan mechanism was deleted; the monitor will print nothing forever. |
| `SignalSender:2999` | javadoc | **false** | snapshotFundingExtremes: "Snapshots symbols whose funding is extreme into ./data + Supabase … reads the funding cache the 15-min refresh already populates; no extra REST." | Two independent breaks: (a) refreshAllFundingRates() (:2431), the only writer of the funding cache via decisionEngine.updateFundingOI, has no caller and is not scheduled anywhere; (b) the loop at :3010 iterates cachedPairs, whose only assignment is :1120 inside the unreachable forceResubscribeTopPairs. rows is always empty → no funding_snaps row is ever written despite BotMain.java:428 calling it every cycle. |
| `SignalSender:3048` | comment | **stale** | "[v87.9] CRYPTO-only forward sample is enforced upstream via HARD_BLACKLIST (stripped from cachedPairs in getTopSymbolsSet) — tokenized stocks/ETFs/metals never reach this tracker." | getTopSymbolsSet (:2542, where the removeIf(HARD_BLACKLIST) at :2547 lives) is invoked only from forceResubscribeTopPairs (:1119, no callers) and initLeverageAndMarginMode (:1461, testnet-only). cachedPairs is never assigned in the live path, so neither the blacklist nor the tracker it protects has any effect. |
| `SignalSender:3076` | javadoc | **false** | trackBreakoutSignals: "Round-robins a few coins/cycle: detects a FRESH daily Donchian breakout on the last CLOSED bar and logs the entry + initial stop." | Called from BotMain.java:429 but returns immediately at SignalSender.java:3082-3083 (`pairs = new ArrayList<>(cachedPairs); if (pairs.isEmpty()) return;`) — cachedPairs is only ever assigned at :1120 inside forceResubscribeTopPairs, which has no callers. No trend_signals row and no alert can ever be produced. |
| `SignalSender:3127` | telegram | **false** | Telegram: "📈 *BREAKOUT-сигналы* (стратегия v5, observation-only) … Дневной Donchian-пробой · бот НЕ торгует · копим форвард-статистику" | Unreachable — the enclosing block only runs if rows is non-empty (:3122), and the loop above never executes because cachedPairs is empty (:3082-3083). Additionally the "бот НЕ торгует" assurance contradicts the live SupabaseSignalBridge path (BotMain.java:414-419). |
| `SignalSender:3247` | javadoc | **false** | flushLiquidations: "Called once per cycle from BotMain.runCycle (gated by BotMain.LIQ_CAPTURE). Drains buffered liquidation events (filled by the WS stream) → ./data + Supabase." | `grep -rn flushLiquidations` → only the definition (SignalSender.java:3249) and two comments (:432, :3524). BotMain's aux cycle calls exactly four SignalSender methods (BotMain.java:427-429, :434: checkNewListings, snapshotFundingExtremes, trackBreakoutSignals, syncServerTime). Nothing ever drains liqBuffer, so captured liquidations are held until LQ_BUFFER_MAX=20000 and then silently dropped at :3525. This is why li |
| `SignalSender:3357` | comment | **stale** | "Single source of truth: used by live scan (selectPairsForScan), startup backtest (getScanUniverseSnapshot) and self-validator (getTopPairsForForecast) so the backtested universe == the live-traded un | All three consumers are unreachable: selectPairsForScan (:1021), getScanUniverseSnapshot (:3664) and getTopPairsForForecast (:3621) have no callers. The live-traded universe now comes from the Supabase bot_orders queue (SupabaseSignalBridge), which passesTradeTier never touches. |
| `SignalSender:3576` | logbanner | **false** | Stats banner "[Stats] cache=%.1f%% early=%d liq=%d corr=%d stale=%d profit=%d e=%d opt=%d vpoc=%d fin=%d isc=%d rej=%d …" | Reports rejection counters that are never incremented anywhere (:594-603 are write-once at construction), so every field except cache/rej/ws/bal is a permanent 0. logCycleStats() (:3571) additionally has no callers. |
| `SignalSender:3602` | javadoc | **false** | getNoiseScore: "Returns the noise score (wick/body ratio) for a symbol from cached 15m candles. Used by EARLY_TICK gate to raise confidence threshold for noisy coins." | Reads candleCache key symbol+"_15m" (:3606), but with the default PRIMARY_TF=1h (:82-83) nothing ever fetches 15m — the only getCached(pair,"15m",…) call is inside getCached15mWithLive (:1196), reachable only when PRIMARY_TF=="15m" (:1189), and that method has no callers either. So it always returns the 1.5 default. There is no EARLY_TICK gate left, and getNoiseScore has no callers. Same defect in getAtr (:3594-3598) |
| `SignalSender:3651` | javadoc | **false** | getCooldownedSymbolCount: "Returns the number of symbols currently in ISC cooldown (post-signal lockout). Used by Watchdog to diagnose signal droughts … E.g. '📭 No signals 90 min (CD=14 pairs locked)' | The body is `return 0;` (SignalSender.java:3657) — a hardcoded constant, not a count. No InstitutionalSignalCore class exists any more, and the method has no callers. |
| `SignalSender:3686` | logbanner | **false** | getRejectionStats: "rej[liq=%d corr=%d stale=%d profit=%d e=%d opt=%d vpoc=%d final=%d isc=%d gic=%d q=+%.0f]" | Same dead counters (:594-603, never incremented); cycleQualityPenalty is only set by refreshCycleQuality (:977), which has no callers. Method itself has no callers. |
| `SignalSender:3697` | javadoc | **false** | getProcessPairBreakdown: "Per-process-pair rejection breakdown — exposed for heartbeat … the INNER funnel inside processPair() that runs before a TradeIdea ever reaches the dispatcher … Counters are c | processPair() no longer exists (grep -rn processPair → only 7 comment hits, no method). Every counter it prints — blockedLiq/blockedCorr/blockedEarlyConf/blockedOptConf/blockedVpoc/blockedFinalConf/blockedIsc (:594-602) — is only ever `.get()`, never incremented (all references: :3577-3580, :3688-3690, :3708-3710). The method also has no callers, so nothing reaches the heartbeat. This is the same failure mode as the  |
| `SignalSender:3791` | comment | **stale** | "depth snapshot (REST /fapi/v1/depth?limit=10) polled every 30s per active pair" | Scheduled at 120s, not 30s (:872, matching the corrected comment at :870), and capped to DEPTH_SNAPSHOT_TOP_N=10 pairs (:1690), not "per active pair". Since wsMap is always empty (:1686) the poll body never executes at all. Note OrderbookSnapshot.isFresh() uses a 30s window (:3814), so even if it ran, snapshots would expire before the next poll. |
| `TelegramBotSender:35` | javadoc | **false** | [F3] ... Prevents "equal content but rebuilt StringBuilder" duplicates that v50 let through. | lastSentText is assigned ONLY in onSuccess() (TelegramBotSender.java:253-257), which is called per SENT CHUNK from sendWithRetry(parts.get(i)) (:162,:219). Two consequences the claim does not survive: (1) for any message >3900 chars splitForTelegram (:336) yields multiple parts, so lastSentText ends up holding only the LAST chunk — the full text never equals lastSentText and such a message can never be deduped (reach |
| `TelegramBotSender:39` | javadoc | **false** | [F5] Queue ... drops LOWEST PRIORITY messages first (not just tail). | dropNonCriticalTail() (TelegramBotSender.java:116-124) copies the queue, walks it from the tail and removes the FIRST element for which !isCritical(m) — it never consults isHighPriority() (:196-209) and does no priority ranking at all. Its own name and its log line (:104 'dropped non-critical tail') say tail. And because isCritical() is unreachable for every currently emitted message (see line 40 finding), the 'non-c |
| `TelegramBotSender:40` | javadoc | **stale** | [F5] Critical messages (TP/SL HIT, DAILY_KILL, FORCE_CLOSE) are preserved even when queue is full. | isCritical() (TelegramBotSender.java:185-194) matches only the literals TP HIT / SL HIT / TP_HIT / SL_HIT / TP1-3 HIT / DAILY_KILL_SWITCH / FORCE_CLOSE / PANIC. Grepping every message actually sent through this sender — the complete set is BotMain.java:476,484,498,511 and SignalSender.java:1605,1635,2673,2705,3127,3196 plus BinanceTradeExecutor.java:2579 via the emergencyAlertSink — none of those strings occurs in an |
| `TelegramBotSender:43` | javadoc | **stale** | [F7] isHealthy() returns false if worker stuck >30s or 10+ consecutive send failures. | There is no isHealthy() in the file (408 lines read in full) nor anywhere in the tree: grep isHealthy over all *.java hits ONLY this javadoc line. `git log --all -S isHealthy` -> added in 046bb2b, removed in ff2a1e8 ('refactor: delete the second layer'), whose commit body literally lists 'Small dead entry points: ... TelegramBotSender.sendMessageSync and isHealthy'. The deleted body was `if(!running) false; if(now-wo |
| `TelegramBotSender:88` | comment | **false** | [F3] Dedup by text equality (normalized). Critical signals (TP/SL HIT, EXHAUSTION, PRE_PUMP, KILL_SWITCH) bypass dedup — they are time-critical. | Three separate errors at TelegramBotSender.java:90-91. (a) 'normalized' — the compare is raw `message.equals(lastSentText)`, no trim/case/whitespace normalization exists in the class. (b) The bypass predicate is isCritical() (:185-194), which does NOT contain EXHAUSTION or PRE_PUMP; those live in isHighPriority() (:204,:207), which only affects queue ordering (:98) and barge-in (:168) and has zero effect on dedup — s |
| `TradingCore:463` | comment | **stale** | "[v13.0 NEW] VOLUME PROFILE ENGINE — Identifies key price levels by where most volume traded. VPOC = ... VAH/VAL = value area" | VolumeProfileResult (TradingCore.java:469) is a DTO with an empty body after the constructor (lines 483-486 are blank where methods used to be) and is never constructed anywhere in the repo. No function computes VPOC/VAH/VAL for it; the only surviving VPOC math is the private ForecastEngine.calcVPOC (1920) inside the dead forecast path. |
| `TradingCore:496` | comment | **stale** | "EXHAUSTION = DON'T enter, move is ending" (trend-phase gate on entries) | The top-level TrendPhase enum (499) and TrendPhaseResult (514) are referenced nowhere outside their own declarations — grep for TrendPhase shows hits only in TradingCore.java, and TrendPhaseResult is never constructed. Nothing on the live order path (SupabaseSignalBridge -> BinanceTradeExecutor) consults any trend phase, so no entry is blocked by exhaustion. |
| `TradingCore:548` | comment | **stale** | "[v13.0 NEW] SWING STRUCTURE ANALYZER... Break of structure (BoS) = strongest signal. Change of Character (ChoCh) = potential reversal" | SwingStructureResult (556) has no producer anywhere in the repo — no analyzer method computes bias/breakOfStructure/changeOfChar. The only structure code still reachable is DecisionEngineMerged.marketStructure (DecisionEngineMerged.java:1590), which returns a plain -1/0/+1 with no BoS or ChoCh notion, called once from TradingCore.java:986 inside the dead forecast path. |
| `TradingCore:576` | comment | **stale** | "[v13.0 NEW] ORDER BLOCK DETECTOR — Order blocks = zones where institutional orders rested. Price tends to react strongly when returning to these zones" | No detector exists: OrderBlock (581) is never constructed and detectOrderBlocks is absent from the repo (see the line 1987 finding). contains()/isNearby() at 593-595 are unreachable. |
| `TradingCore:624` | javadoc | **stale** | v10.5 tied-rank fix note: the `>=` change affects a 0.08 "compression" gate in DE and "All directional indicators in DE that consume this remain semantically correct" | Nothing in DecisionEngineMerged.java (or anywhere) consumes atrPercentile — zero call sites repo-wide. The described 0.08 compression gate / 0.50 mid-vol / 0.90 high-vol consumers no longer exist; the squeeze logic that survives (TradingCore.java:1597 isVolatilitySqueeze) has its own ATR-percentile code and does not call atrPercentile. Same for the companion claim at line 617 about "spurious COMPRESSION flags in DE". |
| `TradingCore:775` | comment | **stale** | VSA engine: "These patterns fire BEFORE price confirmation — they are leading signals" | There is no VSA detector left — VsaResult (780) and VsaResult.none() (815) are never constructed or called anywhere in the repo (grep VsaResult returns only its own declaration lines). No pattern can fire at all, before or after price confirmation. |
| `TradingCore:787` | comment | **false** | DEMAND_ABSORPTION is documented under "── Bullish signals ──" (line 783) as "Narrow UP-bar, very high vol → supply being absorbed into rally" | VsaResult's classifier at TradingCore.java:805-807 sets isBullish only for EFFORT_TO_FALL_FAILED, STOPPING_VOLUME_BULL and NO_SUPPLY — DEMAND_ABSORPTION is omitted, and it is not in the isBearish list (808-811) either, so it evaluates to neither bullish nor bearish. The mirror-image SUPPLY_ABSORPTION *is* included in isBearish (811), confirming the omission is an asymmetry rather than an intentional neutral class. |
| `TradingCore:825` | javadoc | **stale** | ForecastEngine header: "REVERSAL-AWARE Direction Forecaster" whose forecast drives bias/confidence for the bot | `ForecastEngine.forecast(...)` (TradingCore.java:918) has ZERO call sites: grep for `forecast(` across the repo returns only the declaration and a comment. The engine is constructed at SignalSender.java:798 and stored in two write-only fields — SignalSender.java:577/800 `forecastEngineDirect` (never read) and DecisionEngineMerged.java:262/307 `forecastEngine` (assigned by setForecastEngine, never read). ~1100 lines ( |
| `TradingCore:841` | javadoc | **false** | "The exhaustion brain OVERRIDES the trend brain. 5 green candles + exhaustion signals = BEAR forecast (not BULL!)" | In the squeeze branch (TradingCore.java:1050-1064) `dir` is computed as `trendDir*0.25 + ofScore*0.35 + vpocPull*0.20` plus breakout bonuses — exhaustionScore is not read at all on that path, and the branch is evaluated BEFORE the two exhaustion branches at 1065/1071. With isVolatilitySqueeze true (1043), maximal exhaustion cannot override trend. Additionally the whole method is never invoked (see the ForecastEngine  |
| `TradingCore:892` | comment | **false** | "only keys listed here participate in confidence calc... This intentionally excludes exhaustion metadata (VOL_FADE, MOM_DECAY, etc.) which are non-directional and were previously polluting confidence  | The exclusion mechanism works (1120-1121 `getOrDefault(key, 0.0)` + `if (w <= 0) continue`), but EXHAUSTION is itself listed in FW_BASE with weight 2.0 (line 899) and FW_SQUEEZE 1.0 (911), and exhaustionScore is clamped to [0,1] at line 977 — it is never negative. So the largest non-directional metric still participates: at 1126 `Math.signum(v) == dirSign` makes it count as agreement for every bullish dir and as disa |
| `TradingCore:922` | comment | **false** | Cold-start handling: "Ранее: один молчаливый return null... Теперь: явное сообщение когда данных недостаточно" (now an explicit message is emitted when data is insufficient) | TradingCore.java:923-928 are three bare `return null;` statements with no output. TradingCore.java contains no logger, no System.out/System.err and no message-sending code at all (`grep -n "LOG\.\|System\.out\|System\.err\|Logger\|send" TradingCore.java` returns nothing). The cold-start path is exactly as silent as the comment says it used to be. |
| `TradingCore:1042` | comment | **false** | "STEP 4: SQUEEZE detection — block all direction in compression" | The squeeze path does the opposite: TradingCore.java:1050-1064 builds a directional `squeezeBias` from trendDir/ofScore/vpocPull and even adds a +0.18 lead and +0.10 alignment bonus, then `dir = squeezeBias`. The adjacent comment at 1051 states the current intent ("Compression is often the launchpad of the move, not a zero-edge state"), contradicting the STEP 4 header. FW_SQUEEZE (906) exists solely to score directio |
| `TradingCore:1092` | comment | **stale** | Confidence factor weights are "HTF×3, OF×3, SWING×2, EXHAUST×2, VPOC×1.5" and LR_8 / FISHER are among the participating factors | Actual FW_BASE (TradingCore.java:894-904): HTF=2.5, OF=2.5, SWING=1.8, EXHAUSTION=2.0, VPOC_PULL=1.5, plus CVD_RT=4.5, CMF=3.5, ACCEL=3.0, OBV_SLOPE=2.5 — three of the five quoted numbers are wrong and the four highest-weight factors are undocumented here. Neither "LR_8" nor "FISHER" is ever put into the factor map `f` (only MOVE_*, VOL_FADE, MOM_DECAY, WICK_REJ, RSI_EXH, OVEREXT, VPOC_PULL, EXHAUSTION, SWING, OF, HT |
| `TradingCore:1113` | comment | **false** | "if 3+ leading indicators all agree (CMF, CVD_RT, ACCEL, OBV_SLOPE) → strong +8% confidence boost" | TradingCore.java:1151 applies +0.10 (`if (leadingAgree >= 3) conf = clamp(conf + 0.10, 0, 0.85)`), not +0.08, and its trailing comment "// all 4 leading agree" mislabels a >=3 condition. The undocumented second tier at 1152 adds +0.04 for >=2. The only +0.08 in the method is the unrelated HTF+OF bonus at 1141. |
| `TradingCore:1594` | javadoc | **false** | isVolatilitySqueeze: "Fallback: if not enough data for full BB/KC, use improved ATR percentile" — the ATR-percentile method was replaced because "ATR alone doesn't tell if we're in a PRE-BREAKOUT sque | TradingCore.java:1602-1629 runs BB/KC only when n>=50 and returns true only on a positive squeeze; on a NEGATIVE result it falls through to 1632-1645, which runs the ATR-percentile test on every call with n>=30 and can return true on its own (`return currentAtr < p20 * 0.80`). The "fallback" is therefore an independent OR-branch, not a data-shortage path: a bar where full BB/KC data exists and explicitly says "no squ |
| `TradingCore:1718` | javadoc | **stale** | calcHTF weighting: "30% static EMA stack (for trend regimes), 70% recent slope (for reversal detection)" | TradingCore.java:1765: `double result = staticScore * 0.30 + dynamicScore * 0.60 + m15Score * 0.10;` — the slope term is 60%, with 10% diverted to the 15m EMA21/EMA50 sanity term added at 1759-1763 (documented only as "small weight, sanity", never folded into the stated split). |
| `TradingCore:1914` | javadoc | **false** | calcVPOC recency decay: "exponential decay per bar (factor 0.96 per bar back). The most recent 10 bars account for ~65% of the total weight." | With decay=0.96 (line 1931) over the 50-bar window actually used (caller at 1184/1543: `calcVPOC(c15, 50)`), the weight sums are geometric: last 10 bars = (1-0.96^10)/0.04 = 8.38 versus total (1-0.96^50)/0.04 = 21.75, i.e. ~38.5%, not ~65%. ~65% would require decay ≈ 0.90. The extra range-spreading term at 1943 is proportional to the same weight and does not change the ratio. |
| `TradingCore:1981` | comment | **stale** | ADVANCED INDICATORS BLOCK contents list promises 10 working modules, incl. "1. SMC: Order Blocks, Fair Value Gaps, Liquidity Sweeps, BoS/ChoCh helpers", "7. Premium/Discount zones", "9. PreMove patter | Only DTOs survive for items 1 (FairValueGap 1993 — no detector, no producer), 7 (PremiumDiscount 2348 — never constructed), 9 (PreMoveSignal 2398 — never constructed) and 10 (ConfluenceReport 2412 — never constructed); BoS/ChoCh helpers are gone (SwingStructureResult 556 has no producer). The modules that do have code — detectLiquiditySweep (2023), superTrend (2125), kama (2185), bollingerSqueeze (2305), cvdDivergenc |
| `TradingCore:1987` | comment | **false** | "OrderBlock class and detectOrderBlocks(List<Candle>, int lookback) already exist earlier in this file (line ~943). The advanced confluence aggregator below uses that existing implementation" | `grep -rn detectOrderBlocks` over the repo returns only this comment — the method does not exist anywhere. Line 943 is now inside ForecastEngine.forecast (exhaustion scoring). The OrderBlock DTO at TradingCore.java:581 has no producer, and there is no confluence aggregator function at all: ConfluenceReport (2412) is a DTO whose bullFactors/bearFactors lists (2415-2416) are never populated by any code. |

## Ложные и устаревшие — косметика (35)

| Файл:строка | Вид | Вердикт | Утверждение | Проверка |
|---|---|---|---|---|
| `BinanceTradeExecutor:30` | javadoc | **stale** | «Поправлены два метода: sendStopMarketOrder() — теперь шлёт на /fapi/v1/algoOrder, и cancelAllOpenOrders()» | no sendStopMarketOrder() exists any more — it was replaced by sendStopMarketOrderDiag (:1896), which does POST /fapi/v1/algoOrder (:1918). Its orphaned javadoc block (:1869-1885) still documents the deleted name and is not even the effective javadoc, since a second block (:1887-1895) follows it. The cancelAllOpenOrders half is correct (:2299-2313 hits /fapi/v1/allAlgoOpenOrders). |
| `BinanceTradeExecutor:146` | comment | **stale** | «Binance отвергает запросы с timestamp дрейфом > recvWindow (5s).» | every signed request in this file explicitly sends recvWindow=60000, not the 5 s default (:282, :329, :591, :1405, :1500, :1656, :1912, :2431), so the stated 5 s window is not the one in force. |
| `BinanceTradeExecutor:749` | comment | **stale** | «единственная такая логика мёртвая (TradingCore.buildSignal не вызывается на live-пути)» | grep for buildSignal across src/main/java/com/bot returns this comment and nothing else — the method no longer exists at all, so the parenthetical understates the amputation ('not called' vs deleted). |
| `BinanceTradeExecutor:898` | comment | **false** | «entryCid … maker id (also taker id when mode=taker)» | :964-969 always mints a FRESH takerCid for the MARKET path and the comment directly above explains why; entryCid is therefore never used as the taker id, and with EXEC_ENTRY_MODE=taker (the default, :897) it is generated and discarded unused. |
| `BinanceTradeExecutor:1433` | javadoc | **stale** | «PositionTracker uses this so a momentary positionRisk error is NOT misread as "position closed"» | no PositionTracker class exists (grep across src/main/java/com/bot returns comment references only). The real consumers are SupabaseSignalBridge.java:204 and :231, plus internal call sites :909/:927/:977/:1186/:1208/:1346. |
| `BinanceTradeExecutor:1468` | javadoc | **stale** | «Used by PositionTracker for active management (trailing-stop / profit-lock / stagnation exit) — needs current price each poll» | fetchMarkPrice's only caller is :1006 (fill-vs-mark sanity check inside openPositionWithSl). No trailing-stop, profit-lock or stagnation-exit code exists anywhere in src/main/java/com/bot. |
| `BinanceTradeExecutor:1592` | logbanner | **false** | log line «leverage -1109 on REAL <symbol> — ABORT trade (cannot confirm 5x)» | the leverage is env-configurable and clamped to 1..10 (:211-212); the message hardcodes '5x' and misreports whenever LEVERAGE != 5. The abort itself is real (:1591-1593 sets lastInitErrorBody and returns false). |
| `BinanceTradeExecutor:2044` | javadoc | **stale** | «Used by PositionTracker when it detects a position has closed — needed to evict orphan TP/SL algo orders» | cancelAllOrdersOnSymbol (:2053) has zero callers in the repo and PositionTracker does not exist. On the live path orphan algo eviction happens only via the pre-entry cleanup at :874-879, inside closePosition (:1360) and after a verified orphan close (:508). |
| `BotMain:330` | comment | **stale** | "DISPATCHER — the only gateway for trade-signal → Telegram." | Section header over an empty section; no Dispatcher type or dispatch() method exists (BotMain.java:456 records its deletion in v88.2). Telegram output now comes straight from SignalSender (e.g. listings alert SignalSender.java:2706) and from BotMain's own boot messages. |
| `BotMain:596` | javadoc | **stale** | "[PATCH B] Fetch klines with backwards pagination… fetches in 1500-bar chunks… Pacing: 600ms between requests… ~3.6s per pair." | Orphan javadoc — the method it documented was deleted; BotMain contains no HTTP kline fetching at all (its only network code is the ip-api timezone lookup at line 630). Pagination now lives in SignalSender.fetchKlinesDirect. |
| `DecisionEngineMerged:12` | comment | **stale** | Header: "CHANGES vs original: MIN_AGREEING_CLUSTERS = 3 (was 2); MIN_CLUSTER_SCORE = 0.25 (was none); Choppiness Index filter (CI > 61.8/68.0 block)" | Code says MIN_AGREEING_CLUSTERS = 2 (line 186) and MIN_CLUSTER_SCORE = 0.22 (line 192), and both are dead constants. There is no Choppiness Index filter in this file — the only CI implementation is TradingCore.java:641+ (choppinessIndex helper), never referenced from DecisionEngineMerged. |
| `DecisionEngineMerged:769` | comment | **false** | "FIX: deduplicate by semantic group, cap at 5 visible flags" | traderFlags() caps at SIX: `if (result.size() > 6) return result.subList(0, 6);` (lines 854-856), with the adjacent comment at 852-853 saying "Cap at 6 flags max". The two comments in the same method contradict each other; the code does 6. |
| `DecisionEngineMerged:991` | comment | **false** | "Warn trader when SL is very tight relative to ATR." | The trigger reads no ATR at all: `_slPctAbs = Math.abs(slPct)` (992) then `if (_slPctAbs > 0 && _slPctAbs < 0.60)` (993) — a fixed 0.60% of PRICE. The class has robustAtr/getRobustAtr available (614-621) and does not consult them here, so a 0.5% stop on a 3% ATR coin is warned about identically to a 0.5% stop on BTC. |
| `DecisionEngineMerged:1039` | javadoc | **false** | "NOTE: We do NOT escape `*` here because trader flags may legitimately contain emoji-prefixed structure that doesn't include `*`." | _mdEscape does escape '*': line 1050 `if (c == '_' \|\| c == '*' \|\| c == '`' \|\| c == '[' \|\| c == ']')` prepends a backslash. The javadoc describes the opposite of the code (which also escapes '[' and ']', undocumented). |
| `DecisionEngineMerged:1111` | javadoc | **false** | "Grade A = 4+ кластера, prob >= 70, калибратор обучен (n >= 50). → Доверять, торговать обычным размером" (and the Grade A line in the card comment at 942) | computeSignalGrade (lines 1125-1129) can only return "B", "C" or "D" — there is no A branch. The comment at 1119-1124 admits the A factor was dead, but the javadoc block above it still advertises Grade A, and the thresholds it lists for B ("3 кластера, prob >= 60") do not match the code either (clusters>=3 && prob>=70). |
| `DecisionEngineMerged:1135` | comment | **false** | "[MODULE 1 v33] FR MOMENTUM HISTORY ... Needed to compute 2nd derivative (acceleration) of funding rate." | frHistory (1138) is written at 1149-1151 and pruned at 1158, but never read anywhere in the repo. The acceleration actually shipped to FundingOIData is computed at line 1146 from the previous fundingCache entry (`accel = delta - prevDelta`), not from frHistory. The adjacent claim on line 1136 ("Deque bounded at FR_HISTORY_SIZE to prevent memory growth") IS satisfied by 1151/1158. |
| `DecisionEngineMerged:1349` | comment | **stale** | "[v8.4 CLEANUP] ... Оставлены только active helpers: tpComputeVolSma, tpIsMajorCoin." | Neither method exists anywhere in src (grep tpComputeVolSma / tpIsMajorCoin = this comment only). They were removed along with the strategies in v88.2; the comment still lists them as the surviving active helpers. |
| `DecisionEngineMerged:1546` | comment | **stale** | "Calibration now lives entirely in ProbabilityCalibrator (PAV isotonic regression with vol-buckets)." | Directly contradicted by lines 221-225 of the same file ("ProbabilityCalibrator removed. Its write path was dead") and by the absence of the class from src. No calibration lives anywhere in the bot now. |
| `RiskGuard:113` | comment | **stale** | "Public config getters (for status messages / Telegram boot banner)" | getDailyTradeLimit/getMaxConcurrentPositions/getDailyLossLimitPct/getWeeklyLossLimitPct (:114-117) have zero call sites repo-wide (grep over src/main/java/com/bot). The Telegram boot banner they were written for was rewritten to omit them — BotMain.java:494-506 now prints only leverage/risk plus 'Риск-лимиты RiskGuard НЕ применяются' and SupabaseSignalBridge.maxOpen(). |
| `RiskGuard:400` | javadoc | **stale** | "Snapshot for telegram /status and external monitoring." | health() (:431) and HealthSnapshot (:402) have zero call sites repo-wide. There is no /status command: `grep -in '/status\|onUpdateReceived\|command' TelegramBotSender.java` → no matches; the class only pushes outbound messages. The only RiskGuard output reaching a human is statusLine() via BotMain.java:462, a log line, not Telegram. |
| `SignalSender:24` | comment | **stale** | File header: "[REFACTOR] FUNDING_REFRESH: 15min, DEPTH_POLL: 120s, TOP_N=30" | TOP_N default is 60 since v86.11 (:763) and MAX_SCAN_PAIRS_PER_CYCLE default is 60 (:789). FUNDING_REFRESH_MS (:61) is still 15 min but is now unreferenced, since refreshAllFundingRates (:2431) is never called. |
| `SignalSender:315` | comment | **false** | "LIVE_M1_BUFFER_SIZE = 180; // [v36-FIX] 4h of 1m bars from WS ticks" | 180 one-minute bars is 3 hours, not 4. The buffer is trimmed to 180 entries at :2392. |
| `SignalSender:1155` | logbanner | **false** | Log line "[STALE-SKIP] %s blocked for 30min (%d stales in window)" | The skip actually applied on the line above is STALE_SKIP_DURATION_MS = 15 * 60_000L (:631, used at :1154). The message hardcodes "30min". Unreachable anyway (recordStaleEvent has no callers). |
| `SignalSender:1386` | comment | **stale** | "(auto-trade always uses ex.fetchAvailableBalance() directly via BotMain)" | BotMain does not call fetchAvailableBalance; the only callers are SignalSender:1390 (this cosmetic balance refresh) and SupabaseSignalBridge.java:271 (the actual live sizing path). |
| `SignalSender:2426` | javadoc | **stale** | "v61: register w/o tracking (tracking happens inside Dispatcher.dispatch)." | Orphan javadoc with no method under it; no Dispatcher class exists in the source tree. |
| `SignalSender:3618` | javadoc | **false** | getTopPairsForForecast: "Filtered by minimum volume ($30M+)" and "Used by BotMain.runAdvanceForecast() to know which pairs to analyse." | The filter is MIN_VOL_ALT_USD (:3626) = 40_000_000 (:123), not $30M. runAdvanceForecast does not exist anywhere in the source tree, and getTopPairsForForecast (:3621) has no callers. |
| `SupabaseSignalBridge:22` | javadoc | **stale** | "The dead candle brain (DecisionEngineMerged.analyze) is simply not invoked on this path." | Vacuously true but describes a deleted symbol: `grep -rn 'analyze' src/` finds no method named analyze anywhere — DecisionEngineMerged.java only mentions it in stale comments (:609, :657) and SignalSender.java:573. The method was cut in v88.2 (f7e663d). Nothing named analyze exists to invoke on any path. |
| `SupabaseSignalBridge:24` | javadoc | **false** | "Every claim below was re-verified against the code and the live schema on 2026-07-28 (project_state id=4)" | Contradicted by the findings at :33/:100/:164 (TESTNET_BASE_URL bypass in BinanceTradeExecutor.java:203) and :45 (reconcileSent's unconditional patch() at :413-417). The DB-side half of the claim does hold: pg_constraint on public.bot_orders confirms bot_orders_side_long CHECK (side='LONG'), bot_orders_status_ck including 'failed_naked', bot_orders_mult [0.2,1.2] and bot_orders_geom. |
| `SupabaseSignalBridge:94` | comment | **false** | "The only cap actually enforced on the live path today — read by the BotMain boot banner." (maxOpen()) | MAX_OPEN is genuinely enforced (drainOpens :259-262, countActive :317-320). But it is not the ONLY cap on that path: BRIDGE_BALANCE_PER_LEG caps the sizing base in this same file (:273, documented as 'a CAP' at :269), MAX_PENDING_AGE_MIN expires pendings (:255), and BinanceTradeExecutor clamps leverage to 10 (:212), risk-per-trade to 5% (:220), the size multiplier to [0.20,1.20] (DecisionEngineMerged.java:650-652) an |
| `SupabaseSignalBridge:184` | comment | **false** | "poll() runs on a timer; log each distinct gate reason once so severe keeps its meaning." | gateLog() (:185-189) remembers only the LAST message. executionAllowed() emits two different messages in one cycle when BRIDGE_ALLOW_REAL=1 and the gate blocks: the 'IGNORED' line (:169) then the 'BLOCKED' line (:172 or :177). Each differs from lastGateLog, so both are re-logged at SEVERE on every 30s poll (BotMain.java:415-417) forever — the exact spam the comment says it prevents. With BRIDGE_ALLOW_REAL=0 (default) |
| `TelegramBotSender:319` | comment | **false** | Strip *, _, `, and the link markers [ ] ( ) when they look like md links / Remove ... [link](url) markup. | The loop body at TelegramBotSender.java:315-322 has exactly one skip condition — `if (c == '*' \|\| c == '_' \|\| c == '`') continue;` (:320). '[', ']', '(' and ')' hit the default append at :321 and are emitted verbatim; there is no link handling anywhere in the method. Same overclaim in the comment at :312. Effect: a plain-text fallback of a message containing a markdown link still prints [text](url). |
| `TradingCore:6` | javadoc | **stale** | File header: "TradingCore v15.0 — INSTITUTIONAL QUANT + FORECASTING FOUNDATION" | The forecasting half of the file (ForecastEngine, 857-1962) is dead — forecast() has no callers (see finding at line 825) — and the surviving live surface of this file is only Candle, Side, atr() and rsi() (usage census of `TradingCore.<member>` across the other 7 java files: Candle x86, Side x37, atr x5, rsi x3, plus the never-invoked ForecastEngine/AdaptiveBrain constructions). Bot version is v88.2.2 (BotMain.java: |
| `TradingCore:1282` | javadoc | **stale** | calcVolumeFade: "average volume of last 3 bars vs first 3 bars of move" | The window is adaptive, not 3: TradingCore.java:1293 `int w = Math.min(3, Math.max(1, moveLen / 2));`, so a 2-4 bar move compares 1-bar averages. The correcting note is only in the inline comment at 1285; the javadoc contract was never updated. Same pattern (correctly documented) in calcMomentumDecay at 1312-1320. |
| `TradingCore:1666` | javadoc | **false** | calcOrderflow "Returns [-0.6..+0.6]" | TradingCore.java:1705 returns `clamp(s, -0.65, 0.65)`; the three components (1679 x0.30, 1697 x0.50, 1702 x0.20) can sum to 1.0 before clamping, so values in (0.60, 0.65] are reachable and are what a consumer thresholding at 0.6 would mis-handle. |
| `TradingCore:2299` | comment | **stale** | BollingerSqueeze.squeeze = "BW in bottom 10% of last 96 bars" | The window is the `pctileLookback` parameter (TradingCore.java:2305, used at 2325), not a fixed 96, and bollingerSqueeze() has no callers repo-wide, so no 96 is ever supplied. The bottom-10% part does match the code (2332 `squeeze = pct <= 0.10`). |

## Подтверждённые safety-утверждения, НЕ проверенные атакой (25)

Аудит признал их истинными, но состязательная проверка до них не дошла — считать неподтверждёнными.

- `SupabaseSignalBridge:193` — "Resolve against the EXCHANGE truth, never guess: live position -> adopt 'open', flat -> 'failed', read failure (NaN) -> leave for the next sweep"
- `SupabaseSignalBridge:219` — "drainCloses() only ever looks at close_requested" and "The PATCH is conditional on the row still being 'open', so it cannot clobber a close_requested raced in by the liv
- `SupabaseSignalBridge:298` — "closePosition() sends reduceOnly=true, so Binance itself refuses to let the close turn into a new opposite-side position" (and inline "reduce-only, fail-closed")
- `SupabaseSignalBridge:281` — "SL failed AND emergency close failed => a LIVE unhedged position. Never bury this in 'failed'." (failed_naked branch)
- `SupabaseSignalBridge:268` — "Sizing base = LIVE account balance from Binance ... BRIDGE_BALANCE_PER_LEG, if set >0, acts as a CAP on the sizing base, never a substitute"
- `SupabaseSignalBridge:62` — "Raw BINANCE_USE_TESTNET, deliberately left UNPARSED so that 'absent' stays distinguishable from 'explicitly 0'. A missing variable must not select an endpoint by default
- `BinanceTradeExecutor:44` — «NO FUNDS, NO MOVE. Перед каждой сделкой проверяется реальный баланс на бирже.»
- `BinanceTradeExecutor:62` — «HARDCODED LEVERAGE CAP … Даже если кто-то поставит env LEVERAGE=50 — будет применено 10.»
- `BinanceTradeExecutor:751` — «При 5x/SL≤4% гейт [liq-distance] НИКОГДА не срабатывает (нулевое изменение поведения сейчас)»
- `BinanceTradeExecutor:918` — «Cancel the resting limit FIRST so it can't fill after we've also sent a taker order (→ double-fill).»
- `BinanceTradeExecutor:1341` — «[v86.24] Checked read: NaN = read FAILED … Fail CLOSED: return false, keep the SL, let the caller keep tracking»
- `BinanceTradeExecutor:1637` — «@param reduceOnly … so Binance will REFUSE it rather than let it open exposure … body2 deliberately omits reduceOnly — Binance forbids it in Hedge Mode»
- `BinanceTradeExecutor:2116` — «2. If cancel fails → ABORT (don't place a second SL on top of the live one — that creates two SLs which can both fire).»
- `BinanceTradeExecutor:2399` — «3-кратным retry (300ms, 600ms backoff) и пост-верификацией позиции … Если все попытки упали — Telegram alert»
- `BinanceTradeExecutor:2579` — Telegram text «🚨 EMERGENCY CLOSE FAILED … РУЧНОЕ ВМЕШАТЕЛЬСТВО: зайди в Binance и закрой позицию вручную.»
- `BinanceTradeExecutor:2727` — «For SHORT positions SL is ABOVE entry — round UP so we never shrink the protective distance. For LONG, round DOWN.»
- `RiskGuard:160` — "this value can only ever make the DAILY drawdown gate BLOCK harder — it never enables a trade"
- `RiskGuard:558` — "[v86.49] parse ALL fields into locals FIRST; commit to instance fields only once the whole line parsed — a corrupt field then restores NOTHING (not a dangerous half-stat
- `BotMain:452` — "[project_state id=22] RiskGuard is instantiated here and NOWHERE ELSE calls it… the daily and weekly loss limits, MAX_CONCURRENT_POSITIONS, the BTC-crash block and the c
- `BotMain:412` — "[supabase-bridge] … Additive + DEFAULT-OFF (SUPABASE_BRIDGE_ENABLED=0) + testnet-locked"
- `BotMain:472` — "Wire emergency-close failure alerts to Telegram. If SL placement fails AND emergencyClose can't close the position, operator gets alerted immediately."
- `BotMain:486` — Telegram boot banner: "Сделки открываются ТОЛЬКО из очереди ордеров (Supabase → мост → Binance)"
- `BotMain:55` — audit map: "'API ключи в коде' — ЛОЖЬ, все через requireEnv/System.getenv()"
- `SignalSender:386` — New-listings catcher: "It NEVER trades and never feeds the calibrator/decision engine — pure data capture + a Telegram alert. Gated by BotMain.NEW_LISTING_CATCHER (defaul
- `DecisionEngineMerged:647` — "Clamped to [0.20, 1.20] in setter to prevent malformed values" (executorSizeMultiplier)
