# Что нового · 24.09.2026

Интерфейс и инфраструктура. **Торговля не тронута**: входы, выходы, риск, размер, сверка работают как
раньше. Telegram-поток по-прежнему не ходит на биржу торговать: любое закрытие идёт через ту же
очередь, которую исполняет основной цикл.

Проверено офлайн (23.09, вечер):

| Что | Результат |
|---|---|
| Java `gradlew test` | 464 теста, 0 падений (47 наборов) |
| `gradlew shadowJar` | собран, `build/libs/bot.jar` |
| Python сканер | 186 проверок, 0 FAIL (5 файлов) |
| Python ops | 181 проверка, 0 FAIL (6 файлов) |
| `bash -n` все `.sh` | чисто |
| Лаба `pytest` | 307 passed |
| Контракт `scanner_view.json` | файл от настоящего `main()` сканера читается Java-ботом, digest.py и панелью лабы поле в поле |

Живьём пока **не** проверено: см. раздел «Не проверено вживую».

---

## 1. Telegram-бот (боевой)

Первая строка каждого экрана — состояние: 🟢 торгует / ⏸ халт / 👁 наблюдение / 🟠 / 🔴.
Всё по-русски, таблицы влезают в экран телефона, время по Варшаве.

### Команды

| Команда | Что покажет (первая строка) |
|---|---|
| `/menu` | `🟢 Торгует` · `📒 2 позиции · 💰 сегодня +$1.24` + кнопки |
| `/status` | состояние + таблица: кошелёк, маржа, свободно, нереал., позиции L/S, закрытия, сверка, аптайм, сборка |
| `/book` | `📒 Книга: 2 позиции` + таблица `TIA L +4.5% −16.6% +17.4% 31` (P&L, до стопа, до тейка, часы) |
| `/pnl` | `💰 Сегодня +$1.24 (+0.9%)` + 7 и 30 дней. Нетто биржи (сделки + комиссии + фандинг) |
| `/queue` | `⏳ Ждут входа: 6` + кто и почему не вошёл: «нет места», «стоп шире, чем тянет депозит», «пауза после сделки»… |
| `/why ADA` | `📒 ADA · LONG · почему вошли`: сигнал, цена входа, 30д, от макс. 20/90д, объём, стоп + сейчас |
| `/halt` | ставит халт новых входов (выходы работают всегда) |
| `/resume` | снимает халт (набранная команда — сразу, как раньше) |
| `/close ADA` | закрыть одну позицию (можно без `USDT`, если монета в книге) |
| `/close all` | `🧯 Закрыть всё: 2 позиции?` + таблица + кнопки ✅ / ✖️ |
| `/help` | список команд |

В меню «/» в Telegram эти команды появятся сами, с русскими подписями.

### Кнопки

- **Меню под каждым экраном:** 📊 статус · 📒 книга · 💰 P&L · ⏳ очередь · ⏸ халт · ▶️ снять халт · 🧯 закрыть всё.
  Кнопка перерисовывает то же сообщение, чат не засоряется.
- **▶️ Снять халт** — теперь с подтверждением: `⏸ Снять халт?` + причина, кнопка живёт 60 с.
  Если за это время халт сменился на другой: `🟠 Халт уже другой — не снят`.
- **🧯 Закрыть всё** — подтверждение, кнопка одноразовая, живёт 60 с. Закроется всё из списка
  **и всё, что открылось после него**, до исполнения. Во время паузы биржи экран честно пишет:
  `🔴 Биржа на паузе ещё N: закрытия пройдут после паузы. Быстрее — вручную в приложении.`
- **📒 ADA под /book** — кнопка на каждую монету, открывает её `/why`. Ничего не закрывает.
- Кнопки со старых сообщений (до перезапуска бота) не срабатывают: бот отвечает и присылает свежее
  меню (не чаще раза в 30 с).

### Строки о сделках (новое)

Каждый вход и выход — одна короткая строка:

```
🟢 Вход LONG TIA
цена 0.5075 · риск $0.72
стоп −12.9% · тейк +22.6%

✅ Тейк TIA ≈+$1.15 (+22.6%)
держал 1 д 7 ч · 0.5075 → 0.6222

🛑 Стоп ADA ≈−$1.80 (−9.8%)
```

Виды выхода: ✅ тейк · 🛑 стоп · ⏱ по времени · ⏱ тренд кончился · ✋ /close · 🧯 закрыть всё ·
✋ закрыта в приложении Binance · ⏸ дневной лимит · 🔴 ликвидация · 🟠 причина неизвестна.

«≈» = посчитано от цены входа и выхода, **без комиссий и фандинга**. Точная цифра — `/pnl`.

Спокойное английское «Position closed» больше не дублирует эти строки. Тревоги (🔴/🟠) приходят как раньше.

**Тихие часы (по желанию):** `QUIET_HOURS=23-08` — ночью строки о сделках копятся, в 08:00 одно
сообщение `🌙 За ночь: 1 вход, 2 выхода` с итогом. Тревоги ночью **не** задерживаются никогда.
`TRADE_NOTIFY=off` выключает только строки о сделках.

## 2. Сканер

После каждого прохода пишет `/opt/tradingbot-data/scanner_view.json`: очередь с причинами,
BTC к SMA50, шорт-рука, что открыл/закрыл, «почему вошли» по своим позициям. Отсюда `/queue` и `/why`.
Решения сканера не изменились ни на байт (тест прогоняет настоящий `main()` с файлом и без).

## 3. Сервер (ops)

- **Вечерняя сводка `digest.py`** (18:00): первая строка = состояние, таблица денег (счёт, плавающий,
  нетто 24ч/7д/30д, комиссии, фандинг), таблица позиций с триггером и возрастом, сделки за сутки,
  ликвидация = 🔴 первой строкой, очередь сканера, **график 30 дней: счёт против BTC** (картинкой).
- **`vps-deploy.sh`**: сначала офлайн-тесты (красный → стоп, бот не тронут) → проверка диска →
  копия данных в `/opt/backups` (последние 7) → сборка → `:prev` только от образа, который реально
  работал → замена, которую не рвёт обрыв SSH → ждёт готовности + минуту смотрит, что не падает →
  итог на один экран. `rollback` одной командой. Сообщения по-русски.
- **`selfcheck.py`**: правка часов биржи (против −1021).
- Все скрипты сервера теперь в git, байт в байт как на сервере (md5 сверены 23.09).
  `.gitattributes` держит их с LF, чтобы Windows не портил.
- **CI** (`.github/workflows/ci.yml`): Java + Python + `bash -n` на каждый push. Заработает после push.
- `watch-key.md`: ключ только для чтения для сторожа. **Не** менять `LAB_VPS_KEY` — им же ходит ночная копия.

## 4. Починки после дня 23.09

- **Цена 0 в журнале закрытий.** Биржа иногда отвечает на рыночное закрытие «исполнено» с ценой 0 и
  дописывает цену позже: так в журнал попали SUI, SOL, ENS на лимите дня. Теперь при нулевой цене бот
  один раз перечитывает свой ордер и пишет настоящую цену (2 теста; без правки новый тест падает).
- **Лимит дня и «отдых» монет.** Пока стоит лимит дня, сканер каждый час предлагал входы, бот их
  отклонял, и каждая монета получала 2 часа отдыха — после 00:00 UTC часть монет ждала лишнее.
  Отказ из-за халта теперь отдыха не даёт: монета не виновата (4 проверки).
- **Разбор минусов:** `tools/ops/lessons.py` (25 тестов) — готовая функция еженедельного
  «📋 Разбор минусов»; к сводке пока не подключена (ждёт «да» владельца). Отчёт по всей истории:
  `analysis/lessons/REPORT.md`.

## 5. Ноутбук: копия данных + панель

В лаб-боте **@StrategTradeBot**:
- Каждую ночь после 03:00 — копия данных сервера в `C:\Users\Asus_F15\TradingBotBackup\`
  (журнал сделок, леджер, логи сканера, стакан/людность). Только чтение с сервера.
  Если журнал на сервере стал короче: `🔴 Журнал сделок на сервере стал короче`, старая копия
  отложена в `archive\` навсегда.
- **`/panel`** — присылает файл `panel-ДД.ММ-ЧЧ.ММ.html`: состояние, P&L, позиции с расстояниями до
  🛑/✅, сделки, очередь, здоровье. Открывается в браузере телефона, светлая/тёмная тема.
  Кошелёк там пока «—»: на сервере он нигде не хранится.

---

## Как включить

Всё в PowerShell 5.1, по одной команде.

### Шаг 0. Посмотреть и закоммитить (сделать самому)

```powershell
cd C:\Users\Asus_F15\IdeaProjects\TradingTelegramBot
```

```powershell
git status --short
```

```powershell
git add .gitattributes .gitignore .github VPS_SETUP.md example.env src tools
```

```powershell
git commit -m "feat: Telegram-интерфейс, строки о сделках, /queue /why, сводка и выкатка"
```

Лаба (только свои файлы, **не** `verify\select48`):

```powershell
cd C:\Users\Asus_F15\IdeaProjects\StrategyLab
```

```powershell
git add lab/ops lab/bot lab/cli.py lab/watch/vps.py tests/test_ops_backup.py tests/test_ops_dashboard.py tests/test_ops_panel.py
```

```powershell
git commit -m "feat(ops): ночная копия данных бота и /panel"
```

Решение за вами: `VenueContainmentTest` теперь разрешает 5 ops-скриптам назвать боевой
REST-хост (`fapi`) — только для чтения; тест отдельно запрещает им любые ордерные эндпоинты.

### Шаг 1. (По желанию) тихие часы — до выкатки

```powershell
ssh -i "$env:USERPROFILE\.ssh\tradingbot_vps" root@185.183.156.154 'grep -q ^QUIET_HOURS= /opt/tradingbot.env || echo QUIET_HOURS=23-08 >> /opt/tradingbot.env; grep -c ^QUIET_HOURS= /opt/tradingbot.env'
```

Ответ `1` = записано один раз. Файл целиком не печатать: там ключи.

### Шаг 2. Выкатка на сервер

По **`tools/ops/INSTALL.md`, шаги 1–7** (проверка на ноуте → копия дерева → scp → убрать CR →
`vps-deploy.sh` → ops-скрипты → проверка). Главная команда шага 5:

```powershell
ssh -t -i "$env:USERPROFILE\.ssh\tradingbot_vps" root@185.183.156.154 'bash /opt/tradingbot/tools/vps-deploy.sh'
```

Номер сборки ставится сам и виден в `/status`.

### Шаг 3. Перезапустить лаб-бот (для ночной копии и /panel)

```powershell
cd C:\Users\Asus_F15\IdeaProjects\StrategyLab
```

```powershell
.\STOP-LAB.bat
```

```powershell
.\START-LAB.bat
```

### Шаг 4. Проверить в Telegram

1. Боевой бот: `/menu`, потом 📊 📒 💰 ⏳. В меню «/» есть `/why`.
2. `/close all` → нажать **✖️ Отмена** (не ✅).
3. ▶️ проверять только когда халт уже стоит (например, после лимита дня): должно прийти `⏸ Снять халт?`.
4. `/queue` покажет очередь после первого прохода сканера (до часа после выкатки).
5. Лаб-бот: `/help` (есть `/panel`), потом `/panel`.

Первые ~30 с после старта экраны пишут «Запуск», `/pnl` заполнится после первой сверки.

Локально Gradle: `JAVA_HOME` на ноуте указывает на несуществующую папку. На одну сессию:

```powershell
$env:JAVA_HOME = "$env:USERPROFILE\.jdks\ms-21.0.9"; .\gradlew.bat test
```

---

## Не проверено вживую

- **Настоящий Telegram**: HTML-экраны, кнопки, правка сообщений, меню «/», строки о сделках,
  🌙 ночная сводка — только на офлайн-подделке и локальном рендере.
- **Выкатка на VPS**: `vps-deploy.sh` гонялся под Git Bash с поддельными docker/curl; на Debian — нет.
- **Новая сводка digest.py**: ни разу не ходила на биржу и в Telegram (первый живой запуск = INSTALL 6.3).
- **Ночная копия / `/panel`** — живая копия с сервера сделана (23.09), но отправка файла в Telegram — нет.
- **Кнопка ▶️ с подтверждением и 🧯 «плюс открытое после списка»** — только тесты.

## Известные ограничения

- «≈» в строках о сделках — без комиссий и фандинга; у тейка из нескольких частей цифра может врать.
- Выходы, случившиеся пока бот стоял, без цены входа: строка короче.
- Тихие часы живут в памяти: перезапуск ночью теряет накопленное.
- `/why` есть только у позиций, открытых **после** выкатки нового сканера.
- Причина халта показывается как записана (часть по-английски, напр. `operator via Telegram`).
- Кошелёк в `/panel` — «—» (на сервере не хранится; сканер мог бы писать его в `scanner_view.json`).
- Три закрытия лимитом дня 23.09 так и останутся в журнале с `price 0` (новые будут с ценой, см. п. 4).
  Строки о сделках и панель считают такую цену «неизвестна», не −100%.
- `/pnl` за 30 дней берётся из истории доходов биржи постранично; сверить с приложением после
  выкатки (офлайн это не проверить).

---

## Откат

**Бот** — прежний образ одной командой (INSTALL, шаг 8):

```powershell
ssh -t -i "$env:USERPROFILE\.ssh\tradingbot_vps" root@185.183.156.154 'bash /opt/tradingbot/tools/vps-deploy.sh rollback'
```

**Только строки о сделках** — без отката: `TRADE_NOTIFY=off` в `/opt/tradingbot.env`, потом `vps-deploy.sh`
(`docker restart` файл не перечитывает).

**Сводка и selfcheck** — вернуть из `/opt/ops-backup-ДАТА` (INSTALL, шаг 8, «Ops-скрипты»).

**Дерево исходников** — из `/opt/tradingbot.bak-ДАТА` (INSTALL, шаг 8).

**Лаба** — `git checkout -- lab` в StrategyLab и перезапуск лаб-бота; папка `TradingBotBackup` ничему не мешает.

**Код целиком** — до коммита: `git stash`; после: `git revert HEAD`.
